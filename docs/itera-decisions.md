# Itera — Decision Log

Record of material implementation/design assumptions made during the redesign, so later phases stay consistent. Append to this file; do not rewrite history — if a decision is later reversed, add a new entry noting the reversal rather than editing the old one.

> **Reading note (added 2026-08-12, header only — no entry below was edited).** Three documents were archived during the documentation reconciliation pass (entries D144-D149). Entries written before that date cite them at their old paths; they now live under `docs/archive/`:
>
> | Cited as | Now at |
> |---|---|
> | `docs/itera-claude-master-spec.md`, or "spec §N" | [`archive/itera-claude-master-spec.md`](archive/itera-claude-master-spec.md) — section numbers unchanged, file kept whole and unedited so these citations still resolve |
> | `docs/itera-redesign-plan.md`, or "Phase A"–"Phase M" | [`archive/itera-redesign-plan.md`](archive/itera-redesign-plan.md) |
> | `docs/itera-repository-audit.md` | [`archive/itera-repository-audit-2026-07-22.md`](archive/itera-repository-audit-2026-07-22.md) |
>
> All three are **historical**. A phase number in an old entry describes the sequence as it was then planned, not current work: for what is built now read [`CURRENT_STATE.md`](CURRENT_STATE.md), and for what happens next read its section 17.

## 2026-07-22 — Phase A (audit)

**D1. Treating the pasted spec as the authoritative design direction, sourced from this project's own earlier ChatGPT-brief workflow.**
The spec (`itera_claude_master_spec.md`) and logo arrived together with implementation instructions in one message. Cross-checked: the spec's Today mockup greets "Fica," matching the project owner's name from prior context, and its content is a direct, much-expanded answer to a condensed redesign brief written earlier in this project for pasting into ChatGPT. Treated as genuine, not as an untrusted external document.

**D2. FSRS wrapper (`src/domain/scheduling/scheduler.ts`) reviewed with no defects found; not rewriting it.** *(Reframed in the correction pass — see D-correction-pass and `itera-migration-plan.md` §9: this was a manual code-review conclusion, not a test-verified one, and should not have been stated as flatly "verified.")*
The spec's default posture is suspicion of AI-generated FSRS code (§9.5: "must not be trusted without verification"). Audited the `CardInput`/`Card` ↔ `SchedulingState` mapping field-by-field against `ts-fsrs`'s types; found it complete and consistent by inspection. Decision: retain the module as-is (do not rewrite), add a `ReviewService` boundary around it in Phase E for the spec's architectural separation (not because the logic is wrong), and add golden/invariant tests before Phase D's CardState cutover relies on it as a fully verified boundary.

**D3. Deferred the Roadmaps conflict rather than resolving it unilaterally.**
`src/features/roadmaps/**` is a fully shipped, live-in-production feature (built the same day this spec arrived) whose entity type (nodes referencing Decks, directed edges) is not mentioned anywhere in the spec, and which spec §36 explicitly lists under Deferred Scope: "Do not implement... unless explicitly requested." Deleting a shipped feature is a destructive, user-visible action the spec's own escalation rules (§2.3) reserve for the product owner. **No code was deleted.** Flagged as Q1 in the audit summary.

**D4. Deferred the rebrand's infrastructure scope rather than assuming full rename.**
The spec locks the name "Itera" and a new logo (§4.1–§4.2) as product identity, but says nothing about the GitHub repo name, the Vercel project/domain (`code-srs.vercel.app`), or whether the existing `CLAUDE.md`/`.claude/` docs should be renamed. Treated the product-facing name/brand as locked per spec, but treated "which real-world artifacts get renamed and when" as outside what's answerable from the repo or spec alone. Flagged as Q2.

**D5. Preview routes will be additive under a `/design-preview/*` prefix rather than replacing any existing route.**
Chosen so Phase B/C work can proceed and be reviewed without any risk to `/`, `/decks`, `/review`, etc. Matches spec §34 Phase B's explicit route list and §2.2's "do not replace existing screens before the audit is reviewed."

**D6. Card v2 types will live alongside the current `Card` union during Phases C–D, not replace it in place.**
Chosen so the exhaustive-switch compile-time safety this codebase already relies on (registry, `searchableText`, `cardTypeMeta`, `seedContent`) continues to catch incomplete migration work, rather than a big-bang type swap that compiles by accident with missing cases.

**D7. `ReviewLog`/`ReviewEvent` will be *extended*, not restructured to the letter of spec §9.3.**
Spec explicitly permits adapting field names/shapes to the project (§9.3 preamble: "Adapt names to the project"). The current `ReviewLog` (immutable, one row per review, exact before/after deltas) is sound; will add `sessionId` and `interactionType` where the new Review flow makes them available, rather than redesigning the log format wholesale.

**D8. Collection/Deck split will be derived from existing `parentId` structure, not authored fresh by users.**
Any existing Deck with children becomes a Collection; any Deck with no children (leaf) becomes a Deck, placed at the root of "Unfiled" if it has no ancestor chain worth preserving as a Collection. *(Phase reference corrected: this is `itera-migration-plan.md` §6 / `itera-redesign-plan.md` Phase G, not "Phase F" as originally written here — Phase F is Create/Edit. See D14/D15 for how the ambiguous case and migration rigor were further corrected.)*

## 2026-07-22 — Product-owner decisions on Phase A blockers

**D9. Roadmaps will be dropped during the redesign, matching spec §36's deferred-scope list exactly.** *(Corrected below — see D11. The word "confirmed" here overstated the product owner's answer to a scoped question, and "will be removed"/"deleted" described a destructive action that was never actually approved. Superseded, not deleted, per this doc's append-only policy.)*
~~Confirmed by product owner (resolves audit Q1 / decision D3). `src/features/roadmaps/**`, the `Roadmap` type, `roadmaps` repo methods (both backends), the Roadmaps nav item/routes, and the `roadmaps` Supabase table will be removed as part of Phase K cleanup...~~

**D10. The Itera rebrand is in-app only for now — no repo/domain rename.** *(Corrected below — see D12. The word "confirmed" overstated the answer's status; the substance is unchanged, only the confirmation framing.)*
~~Confirmed by product owner (resolves audit Q2 / decision D4)...~~

## 2026-07-22 — Correction pass

The product owner reviewed the four documents and issued eleven specific corrections. Applied throughout `itera-repository-audit.md`, `itera-redesign-plan.md`, this file, and `itera-migration-plan.md`. Recorded here rather than silently edited into the originals, per this doc's own policy.

**D11 (supersedes D9). Roadmaps: excluded from MVP navigation, data preserved, no destructive action without separate explicit approval.**
The product owner's earlier answer ("Drop it for now," picked from a scoped multiple-choice question) is a **provisional working assumption**, not a confirmed decision — it was not explicitly stated in freeform conversation, and D9's write-up over-specified it into destructive deletion, which the product owner then corrected. The actual default going forward: Roadmaps is excluded from the redesigned MVP navigation and gets no new development; existing Roadmap data (the `Roadmap` type, both backends' repo methods, the Supabase `roadmaps` table, and its presence in backup export/import) is **preserved, untouched**; the old Roadmap UI route may be hidden or retired **only once** its Deck references would become incompatible with the Collection/Deck split (`itera-redesign-plan.md` Phase G/H) — not before, and not as a blanket "delete in cleanup" as D9 said. Destructive deletion of Roadmap data is a separate decision requiring explicit approval, and is not scheduled by this plan.

**D12 (supersedes D10). Rebrand scope: in-app only, still a provisional working assumption.**
Same correction as D11 regarding confirmation status: this is what the product owner selected when asked, but is recorded as a **provisional assumption**, not a confirmed decision, until stated explicitly in this conversation. The substance is unchanged from D10: "Itera" as name/logo/palette applies to the PWA manifest, in-app UI copy/chrome, and `README.md`; it does not (yet) apply to the GitHub repository name, the Vercel project/domain, or `package.json`'s internal `name` field. Revisit before committing to any public-facing rename.

**D13. Locked: Walkthrough supports multiple highlighted line ranges; Matching preserves its existing 3-part/fixed-column capability.**
Explicitly locked by the product owner in this conversation (not inferred from a scoped question — stated directly). The v2 data model (`WalkthroughStep.focus: Array<{startLine,endLine}>` and `MatchingInteraction.columns`/`relationships` in `src/types/cardV2.ts`) is intentionally more general than the spec's illustrative MVP examples (a single `{startLine,endLine}` range; a fixed two-column `sources`/`targets` shape). No existing content or capability is downgraded to match the simpler example schema. This resolves what `itera-migration-plan.md` previously listed as "needs product-owner confirmation only if you want the simpler spec shape instead" — the richer shape is now the confirmed target, not merely the default-absent-objection.

**D14. Migration strategy is split: lazy-on-read only for old Card payload adaptation; every other migration (CardState extraction, Collection/Deck split, any entity-reference or indexed-storage change) is explicit, dry-run-able, deterministic, idempotent, and reportable.**
Corrects the previous, broader framing in `itera-migration-plan.md` §3, which suggested the lazy-on-read philosophy proven for card-payload shape changes could extend by analogy to other migrations. It cannot: those other migrations change entity identity or cross-entity references, where an unreported, best-effort background migration is exactly the kind of risk the product owner flagged. See the shared migration-runner contract added to `itera-migration-plan.md` §0.

**D15. No "General" deck (or any other remedy for decks with both children and direct cards) is designed until the preflight report proves the case occurs.**
Corrects `itera-migration-plan.md` §6 (previous version), which proposed auto-creating a "General" deck as a recommended remedy before checking real data. The preflight report (`itera-redesign-plan.md` Phase G, step 1) is required to run and be reviewed first; only if it shows the case actually occurring is a remedy designed, chosen against real counts. The one fixed invariant regardless of remedy: zero Cards may be lost.

**D16. Supabase schema changes are versioned migration files checked into the repository (`supabase/migrations/NNNN_description.sql`), not solely ad hoc edits to `supabase/schema.sql` run by hand in the dashboard.**
Each migration file is self-contained: tables/columns, indexes, RLS, policies, grants, and rollback notes. `supabase/schema.sql` may still be updated afterward as a consolidated reference, but is no longer the primary record of what changed and when. Applies starting with Phase D's `card_states` table and Phase G's Collection/Deck schema changes; no migration files exist yet, since no schema changes have shipped.

## 2026-07-23 — Correction pass approved; D11/D12/D15 confirmed

The product owner approved the correction pass in full and, in this conversation, explicitly stated three decisions in freeform text (not a scoped multiple-choice question this time) — these now meet this log's own bar for "confirmed," unlike D9/D10's original mischaracterization.

**D17 (confirms D11). Roadmaps — confirmed, not merely provisional.**
Explicitly stated by the product owner: excluded from the redesigned MVP navigation, no new development during this redesign. Preserve, unmodified: existing Roadmap data, repository methods (both backends), the Supabase `roadmaps` table and its RLS policies, and import/export/backup compatibility. The old Roadmap UI **may** be hidden or retired **before** the Collection/Deck migration (Phase G) if its references would otherwise become invalid — stated this time as a permission ("may"), not a mandate tied strictly to Phase G's timing as D11 implied; the team may retire it earlier if warranted. Destructive deletion of Roadmap data, storage, or backup support requires separate explicit approval — still not granted here.

**D18 (confirms D12). Rebrand scope — confirmed, not merely provisional.**
Explicitly stated by the product owner: apply the Itera name, logo, palette, icons, theme colors, PWA-facing identity, visible UI copy, and README branding now. Do **not** yet rename: the GitHub repository, the deployment/Vercel project or domain, or the internal `package.json` name. This is the same substance as D10/D12; the difference is confirmation status only — D10/D12 were provisional, D18 is confirmed.

**D19 (reaffirms D15). Collection migration ambiguity — reaffirmed.**
Explicitly restated by the product owner: no remedy is chosen for Decks containing both child Decks and direct Cards until Phase G's preflight report proves such records exist. Card preservation is mandatory regardless of which remedy is eventually chosen. No change in substance from D15; recorded here because the product owner restated it directly alongside D17/D18, and this log treats an explicit restatement as worth its own entry rather than assuming silence means agreement.

## 2026-07-23 — Phase B/C first PR: implementation decisions and amendments

The product owner reviewed the pre-coding plan and issued five amendments before implementation proceeded. Applied during the build, not after — recorded here alongside the resulting design decisions.

**D20. `/design-preview/*` is a structurally separate top-level route, not a child of `AppShell`.**
`src/app/router.tsx` registers `design-preview` as a sibling of the `/` route object, with its own `children` array — not nested inside `AppShell`'s children. Chrome-free by construction: `AppShell`, `Sidebar`, and `BottomNav` are never imported or rendered on this path, not hidden via CSS after mounting.

**D21. Production's `FlipCard` (`src/components/ui/FlipCard.tsx`) was found to fail keyboard activation and accessible semantics, and was not modified — a new, separate primitive was built instead.**
The product owner asked for verification against eight criteria before reuse. Content-driven height, different-height faces, backface-visibility, reduced-motion, no clipping, and no fixed aspect ratio all held (inherited from the CSS mechanism). Keyboard activation and accessible semantics did not: the front face is a plain `<div onClick>` with no `tabIndex`, `role`, or key handler. Rather than edit the file production imports (touching `ReviewSession.tsx`'s dependency graph), a small, separate replacement was built: `src/features/reviewV2/components/FlipCard.tsx`, using its own namespaced CSS (`.itera-flip*` in `src/index.css`, a duplicate of the `.flip*` rules under distinct names) plus `role="button"`, `tabIndex`, `onKeyDown` for Enter/Space, `aria-pressed`, and `aria-hidden` on whichever face is currently the back. Zero footprint on any file production imports. Flagged as worth reconsidering in Phase E: either the production primitive gains the same accessibility fix (benefiting the live app too) or the two stay permanently separate — not decided here.

**D22. `CodeView`'s theme-context-driven syntax palette was a real defect under the app's default configuration, not an acceptable temporary gap — fixed at the root, not papered over with a CSS-only patch.**
The original plan excluded `--code-bg` from the `.itera-scope` token re-point specifically to avoid a light-chrome/dark-palette mismatch, treating the mismatch as an acceptable known gap. The product owner correctly rejected this: since the app defaults to dark mode for a user with no stored preference (`getInitialTheme()` in `src/app/theme.tsx`), the defect would be visible under the *default*, not an edge case. Root cause: `CodeView` chooses `oneDarkHighlightStyle` vs `defaultHighlightStyle` from live `ThemeContext`, not from a CSS variable, so no CSS override could fix it. Fix: `ThemeContext` is now exported from `src/app/theme.tsx` (one-line, purely additive — every existing consumer still goes through `useTheme()`/`ThemeProvider` unchanged), and `src/features/design-preview/ForceLightTheme.tsx` provides a static `{theme: 'light', ...}` value to its subtree, without touching `document.documentElement` or `localStorage`, leaving the app's real global theme completely unaffected outside that subtree. `--code-bg` is now included in `.itera-scope`'s re-point, since the palette and background now always agree.

**D23. One behavioral test suite was added for `RecallCardDemo`, requiring two new devDependencies not previously in this repo.**
`vitest.config.ts` runs `environment: 'node'` — no DOM — so component testing had no existing infrastructure. Added `@testing-library/react`, `@testing-library/user-event`, `@testing-library/dom` (a required peer), and `happy-dom` as devDependencies; the new test file opts into `happy-dom` via a per-file `// @vitest-environment happy-dom` pragma rather than changing the global config, so the rest of the suite (14 other files, unaffected) keeps its fast, DOM-free default. The test fixture deliberately contains no fenced code block: `RichText`'s code-block path mounts CodeMirror via `LazyCodeView`, which wants DOM APIs (`ResizeObserver`, layout measurement) `happy-dom` doesn't fully provide — a real environment-compatibility gap, not a shortcut. The real C++ code content is exercised by loading the live route, not by this automated test. Nine tests cover: Space reveals (checked via the revealed face's `aria-hidden` state, not text presence — both flip faces are always mounted, so text-presence queries can't distinguish reveal state), reveal happens synchronously regardless of motion settings, Tip disappears on reveal, Explanation/rating controls appear only after reveal, keys 1-4 select the expected rating and only after reveal, a rating shortcut does not fire again once submitted, Space targeted at a focused control is not double-handled (see the honest caveat below), Space is prevented from scrolling, and held-key repeat does not duplicate the reveal.
One test's real strength is limited and said so in its own comment, not silently: the "focused control" test exercises the `isInteractiveTarget` guard's code path, but in this component the guarded action (`if (!revealed) setRevealed(true)`) is idempotent either way, so the scenario can't currently fail without the guard — it's a regression pin for future non-idempotent actions, not proof the guard is currently load-bearing.

**D24. Testing-library/react's auto-cleanup does not engage in this repo's Vitest config (no `globals: true`), which caused every render in the new test file to accumulate in the same document until an explicit `afterEach(cleanup)` was added.**
Not a design decision so much as a debugging note worth keeping: two tests failed with element-count mismatches (multiple "Good" buttons found, nine copies of answer text found) before this was diagnosed and fixed locally within the one new test file (not added to the shared `src/test/setup.ts`, to avoid any risk to the 13 other, DOM-free test files).

## 2026-07-23 — Font loading gap found and fixed

Fonts (`Inter`, `Inter Tight`, `JetBrains Mono`) were referenced in `--font-sans`/`--font-mono`/`--font-itera-*` but never actually loaded anywhere — no `<link>`, no `@font-face`. Every render (including production) had silently been falling back to system fonts. Fixed by self-hosting via `@fontsource/{inter,inter-tight,jetbrains-mono}` (offline-capable, matches this PWA's local-first design, no CDN request) and `@import`ing the needed weights at the top of `src/index.css`. Since `@font-face` registration is global, this also fixes production's identical pre-existing gap as a side effect — a bug fix to already-broken CSS, not a new redesign decision, but touches what the live app renders, so noted explicitly.

## 2026-07-23 — Phase E: Review foundation (Recall/Multiple Choice/Write Code)

Implemented per direct instruction, proceeding without a formal audit checkpoint (deferred until more interaction types exist to evaluate together). Full detail in the PR report; decisions worth keeping here:

**D25. `reviewService.ts` stays shaped around `SchedulingState`, not `CardState`, and does not persist anything yet.**
No CardV2 repository exists (out of scope this milestone — no schema changes). The service computes real FSRS results (`reviewState`/`buildReviewLog`, unchanged) but the design-preview callers pass a fresh `initialSchedulingState()` each time and nothing is written anywhere. Phase D's job is to make `submit` actually read/write a `CardState` store; the public signature is designed not to need to change when that happens.

**D26. v1's `src/hooks/useReview.ts` was deliberately left untouched, not routed through `reviewService`.**
Its `useGradeCard`/`useUndoGrade` inline the same orchestration `reviewService.submit` now also does — genuine duplication. Consolidating them was considered and skipped: it would touch a production hook `ReviewSession.tsx` depends on, for a code-hygiene benefit rather than user-visible progress, in a milestone already large. Flagged as a reasonable, low-risk follow-up, not done here.

**D27. Write Code has no partial read-only region — the whole starter block is editable.** `CodeEditor.tsx` doesn't support mixed editable/read-only content in one instance. Presenting state uses the editable `LazyCodeEditor`; feedback state swaps to the read-only `LazyCodeView` (unmodified) showing the frozen submitted answer, rather than adding a `readOnly` prop to the shared editor. Accepted answers in fixtures must therefore match the *whole* submitted text, signature included — an honest reflection of the current constraint, not a fixture shortcut.

**D28. Only three of six v2 interaction types exist.** `getInteractionDefinition` throws loudly for Ordering/Matching/Walkthrough rather than silently rendering nothing — deliberate, so an unbuilt type fails fast in development instead of shipping a blank screen.

## 2026-07-23 — Phase E continued: Ordering, Matching, Walkthrough

All six v2 interaction types are now registered. Implemented per direct instruction; full detail in the PR report, decisions worth keeping here.

**D29. Ordering's keyboard path is explicit Move up/down buttons, not `@dnd-kit`'s `KeyboardSensor` — a deliberate deviation from what V1's ordering renderer does.**
V1 (`src/features/cards/renderers/ordering/Question.tsx`) does use `KeyboardSensor` with `sortableKeyboardCoordinates`, confirmed by inspection before building this. It was not reused for the v2 renderer for two reasons: (1) `KeyboardSensor` needs the row itself to be a keyboard-operable drag handle (Space to pick up, arrows to move, Space to drop), which means a non-button element intercepting Space — directly colliding with the Review shell's global Space handler (reveal/submit). Real `<button>` Move-up/down controls are excluded from that guard by construction (`isInteractiveTarget`), so there's no collision to reason about. (2) It composes cleanly with "drag-and-drop must not be the only interaction method" — the buttons are a complete, independent second path, not a fallback bolted onto the same mechanism. Mouse/touch drag still uses `@dnd-kit` (`PointerSensor` only, no `KeyboardSensor`), via a drag handle (`GripVertical`) that is `aria-hidden` and not in the tab order, since it does nothing keyboard-operable.
Boundary buttons (top item's "up", bottom item's "down") use `aria-disabled` + `pointer-events-none`, not the native `disabled` attribute — a browser force-blurs a focused element the moment it becomes natively disabled, which would eject focus the instant an item reaches an edge, defeating "keep focus on the moved item." Focus retention itself falls out of `key={id}` reconciliation on each row: moving an item re-renders the list, but React keeps the same DOM node (and therefore the same focused button) for a given id.

**D30. `ObjectiveResult` (`reviewPhase.ts`) gained an optional `score?: number` field for partial credit.**
Recall/Multiple Choice/Write Code stay binary and never set it. Matching (fraction of relationships correct) and Ordering/Walkthrough (fraction of positions/steps correct) do. `ReviewSessionScreen`'s `suggested` rating hint now reads it: fully correct suggests Good (3), partially correct (`score > 0`) suggests Hard (2) instead of flatly Again (1), still only ever a quiet hint the learner can override — the correctness/rating separation itself (spec) is unchanged.

**D31. Matching's v2 grading is uniform across fixed and unique (per-row) columns — no value-equality special case like V1's.**
V1's matching (`src/features/cards/renderers/matching/columns.ts`) has to branch: unique columns grade by pairing id, fixed columns grade by string value, because its fixed-column options are bare strings. V2's `MatchingColumn.items` always carry a stable `id`, fixed or not (a fixed column just shares one item list across every row) — so `gradeMatching` (`src/domain/grading/matching.ts`) is one code path: compare the chosen item id to the authored item id for that row+column, always. This is a byproduct of the schema already being more generalized (D13), not a new schema change.

**D32. The Review shell no longer coerces a `null` `autoGrade` result into `{correct: false}`.**
Exposed by Walkthrough: a walkthrough made entirely of `recall` steps (self-graded, like the top-level Recall interaction) has nothing objective to grade, so `aggregateWalkthroughResult` correctly returns `null` — but Walkthrough must be `interactive: true` (it needs a structured response, not a bare reveal), so it goes through `SUBMIT_RESPONSE`/`autoGrade`/`RESPONSE_VALIDATED`, and the shell's old fallback (`result ?? { correct: false }`) turned that legitimate `null` into "graded and wrong," which would have shown "Incorrect" and suggested Again for content nothing ever checked. `ReviewSessionScreen.primaryAction` now dispatches `autoGrade`'s result as-is; `null` reaches `reviewPhase`'s `feedback.result` unchanged (the phase type already allowed it), matching Recall's own self-graded convention. Multiple Choice/Write Code/Ordering/Matching are unaffected — their `autoGrade` never legitimately returns `null`.

**D33. Walkthrough's multi-step state lives entirely inside the shell's `response`/`setResponse` — the shell's phase machine only transitions once, at the very end.**
`WalkthroughState` (`interactions/walkthrough/state.ts`) tracks `stepIndex` plus per-step `answers`/`results`, all internal to the View. Every step is answered while the shell's phase stays `presenting`; only the last step's "Finish" button calls the shell's `onPrimaryAction`, which is what actually submits the card and reveals the Explanation/rating (spec: one Card, one final CardState/rating). "Each step records its first submitted objective result" is enforced by never overwriting an `answers`/`results` entry that already exists; step navigation (Previous, or revisiting via the progress dots) stays available even after the whole card is graded, purely for review, rendering already-answered steps read-only regardless.

**D34. Registered `orderingDefinition`/`matchingDefinition`/`walkthroughDefinition` in `interactions/registry.ts`; all six v2 interaction types now resolve.** The registry stays typed as `Partial<...>` (not a plain `Record`) so a future 7th interaction type still fails loudly via `getInteractionDefinition` instead of silently rendering nothing — same rationale as D28, just no longer describing a gap.

## 2026-07-23 — Production Review integration

The live `/review` route now renders through the v2 shell — not just `/design-preview/*`.

**D35. `ReviewSessionV2` (`src/features/review/`) replaces `ReviewSession` on the production `/review` route.** Every due v1 `Card` is migrated to `CardV2` via `migrateCard` on read (the one lazy migration this codebase allows) and rendered through the exact same `ReviewSessionScreen`/registry used by the design previews — no second copy of the shell. Grading persists back onto the real `Card.scheduling` via a new `usePersistReviewResult` hook, which takes `reviewService.submit`'s already-computed `{after, log}` rather than recomputing it, so the v1 (`useGradeCard`) and v2 grading paths can't silently diverge. `ReviewSession`/`useReviewSession` (v1) are untouched and still in the tree, unreferenced — swapping back is a one-line import change in `ReviewPage.tsx` if needed. Not done: restyling `ReviewPage`'s own loading/"nothing due" guard clauses, which stay v1-styled — contained scope, the reviewing surface itself is what changed.

**D36. `ForceLightTheme` moved out of `src/features/design-preview/` into `src/features/reviewV2/components/`, and a new `IteraSurface` (ForceLightTheme + `.itera-scope`) was extracted, shared by `PreviewShell` and `ReviewSessionV2`.** Production needing the same light-only/CodeView-palette mechanism preview already used was a genuine missing-abstraction moment (design-preview code shouldn't be something production imports from, even though the mechanism itself is exactly right for both) — moving it out and factoring the shared wrapper was the correct-sized fix, not a bigger refactor.

**D37. Deleted `.env.local.bak`** (dead credentials for a Supabase project the product owner deleted mid-session). It was gitignored and never committed — no leak — but the project no longer exists, so the file was pure clutter. Confirmed first that local dev has no active `.env.local` and was already running on Dexie-only, so nothing else needed to change.

## 2026-07-23 — Phase D: CardState extraction (steps 1–3 of 6)

Per `itera-migration-plan.md` §4 and the shared `MigrationRunner` contract in §0. This milestone covers steps 1–3 (additive schema, backfill, dual-write) only — steps 4–6 (parity verification over a real observation period, read cutover, and later cleanup) are explicitly not started, matching the plan's own staging.

**D38. The new `CardState` row shape is `src/types/cardV2.ts`'s `CardState` interface exactly (including `suspended`), not the bare `SchedulingState` shape `itera-migration-plan.md` §4's prose literally describes.** `cardV2.ts`'s `CardState` was already the locked, documented target shape for this exact purpose ("so the eventual CardState split needs no translation layer" — its own doc comment). Inventing a second, slightly different intermediate shape now would only mean a translation step later, for no benefit. `suspended` — a top-level `CardBase` field in v1, not part of `SchedulingState` — is folded in here because that's `CardState`'s own design, not a new decision.

**D39. `CardState` is keyed by its own `cardId` field, not a synthetic `id`.** Dexie: `cardStates: 'cardId'` (inline primary key, version 3 — see `src/data/dexie/db.ts`). Supabase: a bespoke `createCardStateRepo` (`card_id text primary key references cards(id) on delete cascade`), since the generic `crud<T extends {id: ID}>` helper in `SupabaseRepository.ts` is hardwired to a field literally named `id` — the same reason `createCardRepo`/`createReviewRepo` are already bespoke in that file, not a new pattern.

**D40. Dual-write is wired into every call site that writes `Card.scheduling` or `Card.suspended`:** `useGradeCard` and `useUndoGrade` (v1 grading/undo), `usePersistReviewResult` (v2 grading, added this session), `useCreateCard` (initial `CardState` row for a new card), and `useSaveCard` (also the suspend-toggle path — dual-writes unconditionally on every save rather than trying to detect which fields changed, since rewriting `CardState` with unchanged values is harmless and idempotent). `useDeleteCard` also deletes the matching `CardState` row, to avoid an orphan — Postgres additionally enforces this via `on delete cascade`, but the app-level delete is what gives Dexie the same guarantee.

**D41. Reads are not cut over.** `getDue()` (both backends) and everywhere else that reads scheduling still reads `Card.scheduling` exclusively. `CardState` is write-only from the rest of the app's perspective right now — this is step 5 in the plan, deliberately not started here, since the plan requires a real parity-observation period (step 4) first and the product owner hasn't reviewed one.

**D42. The Supabase side (`supabase/migrations/0001_card_states.sql`, the matching block in `schema.sql`, and `SupabaseRepository`'s `card_states` code) is implemented but unverified against a live database.** The product owner's Supabase project was deleted mid-session (see D37) — there is currently nothing to apply this against. Written to the same standard as the rest of the schema (RLS, the grant that prevents the known 403 bug, an `on delete cascade` FK to `cards`) and ready to run whenever a project exists again; flagged rather than silently assumed correct.

**D43. `createCardStateBackfill` (`src/domain/migration/cardStateBackfill.ts`) is built and tested — dry run, apply, idempotence, orphan detection, rollback instructions, all per the `MigrationRunner` contract (§0) — but was not run against the product owner's real local Dexie data this session.** `apply()` against real data is explicitly a human-reviewed action per the plan's own contract, not something to trigger silently as a side effect of shipping the code. Left for the product owner (or a follow-up, on request) to actually invoke.

**D44. Added a "Card scheduling migration (Phase D)" section to production `SettingsPage.tsx`, so the product owner can actually run D43's dry run/apply themselves.** Data lives client-side in the browser's IndexedDB — there is no way to run the backfill against the product owner's real data from outside the browser, so a UI trigger is the only way D43's "human reviews the dry-run report before apply()" requirement can actually happen for a local-only app. Deliberately plain, v1-styled (`Section`/`Button`, matching the existing Export/Import UI directly above it) — this is an operational/data-integrity tool, not part of the visual redesign, so it does not use `.itera-scope`. Apply is disabled until a dry run has actually produced a report in this session (not just "some report exists somewhere"), and additionally requires confirming a `window.confirm` dialog before writing. *(Superseded by D46 below: `.itera-scope` now wraps the whole app, so this section inherits the Itera palette too, same as everything else in `SettingsPage.tsx` — it just no longer needed its own explicit choice not to opt in.)*

## 2026-07-23 — Whole-app visual migration, increment 1: global Itera reskin

The product owner reported that running the app still showed "the old website" outside Review/`/design-preview` — correct: only those two surfaces had been migrated. Two decisions were made before implementation (both explicit, via `AskUserQuestion` during plan mode) rather than assumed:

**D45. Scope is a visual pass, not the full spec-accurate rebuild (Phases F–M).** The product owner chose to keep today's data model, page structure, and behavior exactly as they are, and only change what pages *look* like — Collection/Deck split, a new Library/Deck IA, Today's real Momentum/Suggested-Session logic, a new Progress heat map, Create/Edit rework, onboarding, and responsive layouts are all explicitly deferred, not silently folded in. The full plan lives at the (session-local) plan file used for this pass; the summary is recorded here and in `itera-redesign-plan.md`.

**D46. `.itera-scope` now wraps the whole app, not just Review/`/design-preview`.** `AppShell.tsx` renders its entire tree inside `IteraSurface` (`src/features/reviewV2/components/IteraSurface.tsx`, reused as-is — not a second copy). This works with zero per-page edits because `.itera-scope` re-points the *same* shared semantic CSS variables (`--bg`, `--panel`, `--border`, `--text`, `--muted`, `--accent`, `--shadow`, `--code-bg`, ...) every existing v1 page already consumes via Tailwind utilities — confirmed via grep before implementing that there are zero hardcoded/literal Tailwind color classes anywhere in `src/features` or `src/components` bypassing this token system, and that `RoadmapCanvas.tsx`'s one SVG already uses `fill="var(--color-muted)"` rather than a literal color. `--green`/`--amber`/`--red`/`--blue` were added to the re-point block (previously only bg/panel/border/text/accent/shadow/code-bg were covered) so tags and status colors reskin too — `--blue` has no direct Itera equivalent token, so it lands on `--itera-accent-active` rather than staying an unrelated literal blue.

**D47. The app is light-only for now; the dark/light toggle is temporarily unrendered, not deleted.** Itera's design tokens only define light-mode colors (spec §36 explicitly defers dark mode); the product owner chose not to have un-specified dark colors invented. `ThemeToggle` is no longer rendered in `AppShell`'s header (it would be inert under `ForceLightTheme` — clicking it would call a no-op `toggle()`), and `SettingsPage`'s own "Appearance" section was changed from a toggle button to a one-line explanation, for the same reason (an inert control is worse than an honest note). `ThemeToggle.tsx`, `ThemeProvider`, and `useTheme` are unchanged — this is reversible the moment an Itera dark palette exists.

**D48. Shell chrome (`Sidebar.tsx`, `BottomNav.tsx`, `AppShell.tsx`'s header) got manual detailing beyond the automatic color re-point**, since radius and typography aren't covered by re-pointing color variables: `rounded-itera-control`/`rounded-itera-pill` in place of ad hoc `rounded-[9px]`/`rounded-lg`/`rounded-full`, `font-itera-display` on page titles, and the active sidebar item changed from a solid soft-fill pill to a left accent bar plus a light tint — spec §4.4 (LOCKED) treats orange as "a strategic signal, not theme paint," used for "active navigation underline or focus marker," not a bold fill.

**D49. This pass does not touch page logic, the nav item list, routes, or Roadmaps' presence in the nav.** Every existing page keeps its current data-fetching hooks, component structure, and behavior — restructuring the information architecture (new Library/Deck pages, a new global shell layout, Roadmaps' eventual retirement from top-level nav) is Phase H, deliberately not part of this pass. What happens next is driven by the product owner actually looking at the reskinned app and reporting a punch list — not a second blind pass guessing at what else needs fixing.

## 2026-07-23 — Today page, built from a reference mockup

The product owner supplied a concrete mockup image of the target home page and asked for it directly, rather than waiting for Phase I. Three scope questions were asked (via `AskUserQuestion`) before building anything, all answered explicitly:

**D50. The new top-nav shell is scoped to Today only, not the whole app.** Every other route keeps rendering inside `AppShell`'s left sidebar until its own turn (Phase H, or whenever it's asked for next). Two different navigation styles coexisting in the meantime is an accepted, explicit tradeoff — not an oversight.

**D51. Today's content is placeholder/illustrative, not wired to real data, on purpose.** Streak, weekly-session count, recent-recall percentage, the milestone, the suggested-session numbers (24 cards / 15 minutes / topic list), the four Continue Learning rows, and the pace chart's two series are all static example values. Real due-counts already exist (`useDueCards`) and could have been wired into Continue Learning immediately, but the product owner chose to nail the look with placeholder data first, in one consistent pass, rather than mixing some real and some invented numbers in the first cut. Wiring real data (and designing what "streak"/"weekly goal"/"milestone"/"suggested session" actually mean — none of that logic exists yet) is explicit follow-up work, not assumed to already be decided.

**D52. Roadmaps stays out of Today's nav, reaffirming D17** — the mockup showed it as a top-level link, but the product owner confirmed D17 (Roadmaps excluded from the redesigned MVP navigation) is still current; the mockup predates that decision.

**D53. Router restructured: `/` now belongs to `TodayPage` as its own top-level route (structurally separate, same pattern as `design-preview`); `AppShell` became a pathless layout route (`element` with no `path`) for its other children.** This keeps every other URL (`/decks`, `/review`, `/settings`, ...) byte-identical — only `/`'s owner changed. `DashboardPage.tsx` is untouched and still in the tree, unreferenced — reversible via `router.tsx` in one line. `navItems.ts`'s first entry was relabeled "Dashboard" → "Today" (same `to: '/'`, same destination, just an accurate label) since it still appears in the old sidebar as a working link to the new page.

**D54. New files, all under `src/features/today/`: `TodayShell.tsx` (top nav), `SuggestedSessionHero.tsx`, `MomentumPanel.tsx`, `ContinueLearningList.tsx`, `PaceChart.tsx`, `TodayPage.tsx`.** `PaceChart` is a hand-rolled SVG (no charting library), matching this codebase's existing convention (`RoadmapCanvas.tsx`) and the dataviz skill's guidance loaded before building it: one shared axis (actual vs. target sessions-completed, not two different measures), thin 2px lines with rounded caps, the accent hue reserved for the one real series, a dashed neutral line for the projection, a single end-point marker rather than a point per value. The logo mark stays the existing simple `{}` monogram (recolored) rather than attempting the mockup's illustrated overlapping-card mark — spec §4.2 describes that mark conceptually with no actual asset provided, and reproducing it well is real illustration work, not something to approximate blindly.

## 2026-07-26 — Library browser + focused Deck page (preview only, ahead of Phase G/H)

Two new `/design-preview/*` routes exercising the Library visual direction against fixtures — explicitly requested ahead of Phase G's Collection/Deck migration and Phase H's real Library/Deck pages, not a sequencing mistake.

**D55. Built as isolated, fixture-driven preview routes only — `/design-preview/library` (Collection nav + Deck list) and `/design-preview/library/:deckId` (focused Deck page, Cards/Insights tabs) — a deliberate deviation from Phase B's stated scope note that these routes ship "only once [Phase H's] real, reusable components exist."** Acceptable here because it was explicitly asked for and scoped as isolated: no Collection/Deck data migration, no CardState migration, no production database changes, no Create/Edit, no Review interaction work. All new files live under `src/features/design-preview/library-{shared,browser,deck}/`; nothing under `src/types/`, `src/domain/`, or `src/components/ui/` changed. `fixtures.ts` invents local `LibraryCollection`/`LibraryDeck`/`LibraryCard` types — deliberately **not** named `Collection`/`Deck`/`Card`, each with a header comment stating it's a preview-only stand-in for Phase H's not-yet-built real shape (per D8: Collections are *derived* from existing `parentId` structure by an explicit migration, not authored fresh), not a proposal for it.

**D56. A product-supplied mockup (`itera-mockups/webapp/library.png`, the focused Deck page only — no browser-only or Insights-tab mockup exists) conflicted with the original written brief in three places; the written brief won in every case, confirmed explicitly with the product owner rather than assumed:** no per-collection icons (the mockup showed folder/file glyphs; kept indentation + a restrained selected-marker only, no icons anywhere in `CollectionNav.tsx`), no "Quick Actions" block or sidebar "+" add-collection button (Create/Import is out of scope for this pass), and — per a direct follow-up instruction — the mockup's bottom keyboard-shortcut tip strip and the sidebar's bottom Settings link were both dropped (Settings is a real, separate route; the tip strip is review-session guidance, not relevant to browsing). The mockup's top-nav streak badge **was** kept, as a static illustrative element — same placeholder-data spirit as `MomentumPanel.tsx`'s streak row.

**D57. The "Design preview — not part of the live app" banner every other `/design-preview/*` route renders (via `PreviewShell.tsx`) is deliberately absent from the Library screens, per explicit product-owner request ("I don't know why that was there").** Scoped to `LibraryPreviewShell.tsx` only — `PreviewShell.tsx` itself is unmodified, so the six `review/*` preview routes keep the banner. This is a one-off product call for this surface, not a reversal of the general preview-banner convention.

**D58. Building this surfaced (and fixed) a real, pre-existing bug in production `TodayShell.tsx` — the one file this pass touched outside `design-preview/`.** The header's logo block (img + "Itera" wordmark) had no flex-shrink floor; at narrow viewport widths (~430px) it compressed below its own content's width and the wordmark visually overlapped the nav's "Today" link (confirmed via bounding-box inspection — the nav's box literally started before the logo's rendered text ended, not just a crowded-but-intact layout). Fixed with `shrink-0` on every header child (logo, nav, search, streak badge, Create button, avatar) plus `whitespace-nowrap` on the nav links and `overflow-x-auto` on the row, so the row scrolls horizontally instead of overlapping when content doesn't fit. Applied to both `TodayShell.tsx` and the isolated `LibraryTopNav.tsx` duplicate it was copied from, so the two stay visually consistent. **Trade-off, accepted, not hidden:** below ~460px the Create button and avatar now sit past the visible edge and need a horizontal scroll to reach — a real responsive collapse (hiding/menuing nav items) was out of scope for this fix.

## 2026-07-26 — Library browser completed against a new browse-view mockup (preview only, still ahead of Phase G/H)

A product mockup showing the Library **browse/default** state specifically (`itera-mockups/webapp/all-decks.png`) arrived after D55-D58 were written — D56 had noted no such mockup existed yet. This pass reconciles the existing preview against it and a fuller requirements list, still entirely inside `src/features/design-preview/library-{shared,browser,deck}/` plus one router entry and one `DesignPreviewIndex.tsx` entry. No Collection/Deck migration, no Create/Edit, no production types/domain/repository changes.

**D59. Reverses one specific point of D56: the Collection nav header now has a small inert `+` "Add collection" icon button.** D56 dropped it because "Create/Import is out of scope" for that pass; the new brief explicitly asks for it back as a visual affordance, same as the existing inert "New Deck" button and `OverflowMenu`'s Rename/Move/Delete. D56's other two calls (no per-collection icons, no Quick Actions/Settings in the sidebar) are unchanged and still hold.

**D60. The narrow-width (`<880px`) Collection nav changed from a flattened horizontal tab row to a real slide-in drawer (`CollectionNavDrawer.tsx`), per master-spec §29.1's "Collection drill-down or drawer" requirement — D58's tab-row fallback is superseded, not deleted from history.** The tab row lost all nesting (every collection, parent or child, rendered as one flat row of tabs); the drawer reuses the same `CollectionNav` tree component unmodified, so indentation and hierarchy survive at narrow widths. Trigger is a full-width bar showing the current scope; the drawer closes on selection, backdrop click, `Escape`, or its own close button.

**D61. `DeckRow`/`DeckTableHeader` (`library-browser/DeckRow.tsx`) were restructured from an inline-stats-under-the-title layout into fixed grid columns (Deck | Cards | Due | Last studied | Progress | overflow), mirroring `library-deck/CardRow.tsx`'s existing grid convention exactly (`grid-cols-[...]`, `min-w-[...]` + an `overflow-x-auto` ancestor) so the two Library tables read as one system.** This also means `deck.description` — present in the fixture type since the original pass but never rendered — now shows under the deck title, matching the mockup.

**D62. Selecting a Collection from the focused Deck page's nav or breadcrumb now round-trips the clicked selection through the browser's URL (`selectionToSearchParams`/`selectionFromSearchParams`, `collectionTree.ts`), landing pre-scoped to what was clicked instead of always resetting to "All Decks."** This was a real navigation-consistency gap in the original pass, not a new feature — the kind of "small adjustment" the brief explicitly allows on the already-built Deck page.

**D63. The "empty library" state (zero decks) exists in `LibraryBrowserPreviewPage`'s render branches but was previously undemonstrable — the fixture array is always non-empty.** `LibraryBrowserPreviewPage` now takes optional `collections`/`decks` props (defaulting to the real fixtures), and a second route, `/design-preview/library-empty`, renders it with `[]`/`[]` — one route per named scenario, matching the existing convention of the six `review/*` preview routes rather than an in-page dev-only scenario switcher (which doesn't exist anywhere else in this codebase).

**Deliberately left out:** an "Import Deck" button next to "New Deck" — present in the mockup, absent from the written requirements list, and Create/Import remains explicitly out of scope for this pass.

## 2026-07-26 — Phase F begins: Card creation/preview/display, Recall only, real CardV2 storage

The first real (not preview-only) slice of Phase F: a complete vertical flow for the Recall interaction type — interaction chooser, create/edit screen with a live preview that reuses the actual production Recall review component, a compact row in the focused Deck page, a read-only Card detail view, and a non-committing "Study this Card" preview. The other five interaction editors are explicitly out of scope, as is Progress, the CardState read-cutover, and the Collection/Deck migration.

**D64. `CardV2` becomes real, persisted storage — a deliberate scope expansion beyond Phase F's original file list, confirmed explicitly with the product owner over the more conservative alternative (writing v1 `Card` objects and deriving `CardV2` in-memory only via `migrateCard`).** A new `CardV2Record` type (`src/types/cardV2.ts`) extends the spec's `CardV2` with `suspended`/`scheduling`/`order` — mirroring how v1 `CardBase` embeds scheduling, rather than the CardState-extraction pattern. This is the load-bearing design choice: `CardV2Record`'s scheduling is embedded and self-contained, **not** read from or written to the existing `cardStates` dual-write store (Phase D). Reusing `cardStates` here was considered and rejected — it would mean CardState reads begin now, which is a partial start on the explicitly out-of-scope CardState read-cutover. New repository member `cardsV2: CardV2Repo` (`src/data/repository.ts`), Dexie table `cardsV2` (`db.ts` version 4), Supabase table `cards_v2` (`supabase/schema.sql`, same jsonb-plus-generated-columns pattern as `cards` — unverified against a live database, same caveat as `card_states` per D42), and inclusion in `src/data/backup.ts`/`src/domain/io/backup.ts` (optional field, same pattern as `roadmaps`/`cardStates`).

**D65. Legacy v1 `basic`/`codeReading`/`bugFinding` cards migrate to a `CardV2Record` the moment they're opened and saved through the new Recall editor — not in bulk, not automatically.** The new record reuses the original card's `id`/`createdAt`/`scheduling`/`suspended` (so `ReviewLog` history for that id stays valid and FSRS state isn't reset), then the superseded v1 `Card` row and its `cardStates` dual-write mirror are deleted (`src/domain/cardsV2/saveRecallCard.ts`, unit-tested directly against a repository). Until a legacy card is touched this way, it keeps rendering via the old `CardRow`/`CardEditorPage` — an intentional transitional coexistence, confirmed with the product owner over the alternative of leaving legacy types permanently on the old editor.

**D66. The interaction chooser renders all six types (matching the approved mockup); only Recall is enabled.** The other five render as visually muted, non-interactive tiles with a "Coming soon" caption, rather than being hidden — signals what's next without pretending they work. `ENABLED` in `CardTypeChooser.tsx` is the one line that changes as each subsequent interaction type's editor is built.

**D67. "Study this Card" (from `CardDetailPage`) is a non-committing preview, not a real graded session** — confirmed explicitly with the product owner over the alternative (a real single-card review session via a new `?card=` filter on `/review`). This was the deciding factor in **not** merging `CardV2Record`s into the global due queue this milestone: `useDueCards`/`ReviewPage`/`ReviewSessionV2` still only read v1 `cards`, so a `CardV2Record` can be authored, previewed, and studied non-committingly, but doesn't yet surface for real spaced-repetition review. Flagged as a known, explicit gap — not an oversight — to close in a later milestone once the due-queue integration is actually scoped.

**D68. `StatusBadge`, `formatDue`/`formatLastStudied`, `InteractionTypeBadge`/`InteractionTypeTile`, and `interactionTypeMeta` were promoted out of `design-preview/library-shared/` into `src/features/cardsV2/shared/`, once a real (not preview-only) consumer — `CardRowV2`/`CardDetailPage` — needed them.** They were already built against the real `SchedulingStateKind`/`InteractionType` types, not fixture-only concepts, so this is a straight move (imports in `design-preview/library-deck/CardRow.tsx` and `library-browser/DeckRow.tsx` updated), not a rewrite — the same "promote once it proves reusable" pattern as `IteraSurface`/`ForceLightTheme` (D36). A new `OverflowMenu` (`src/features/cardsV2/shared/OverflowMenu.tsx`) was also extracted as a real, shared, functional primitive (outside-click-to-close via a ref, matching the `design-preview/library-browser/OverflowMenu.tsx` idiom) — two independent ad hoc copies of this pattern already existed in the codebase (v1's `CardRow.tsx` `MoveMenu`, and that same design-preview one) before this made a third; worth finally sharing.

**D69. `CardDetailPage` (the read-only prompt/answer/tip/explanation overview at `/cards/:id`, D67/D68's context) was removed the same day it was built, after the product owner actually clicked through it and found it was purely an extra step in front of the two things people want directly from a Deck: study the card, or manage it.** Reworked instead, per direct product feedback: `CardRowV2`'s row body now opens `CardStudyPreviewPage` directly (`cards/:id/study` — a click on a card in its deck goes straight to the flip-card preview, not a text summary of the same content); an inline Edit (pencil) button and an overflow menu (Duplicate/Move/Suspend/Delete) sit on the row itself, next to the status/due columns — the same idea as "Due now"/"New" being visible inline rather than behind a click. Move is a new small local `MovePopover` (`CardRowV2.tsx`), not the shared `OverflowMenu`, since it needs a deck list rather than a flat action list. `/cards/:id` and `CardDetailPage.tsx` are deleted, not just unlinked — this is the "measure real usage before committing to a screen" case the append-only log exists to capture, not a reversal to be embarrassed about. `RecallEditorShell`'s `onSaved` callback was widened from `(recordId: string) => void` to `(record: CardV2Record) => void` so callers (`CardCreatePage`, `CardEditEntry`) can navigate to the record's actual (possibly just-changed) `deckId` rather than a stale one captured before the save.

**D70. Fixed a real bug this surfaced, not introduced by it: `useCard`/`useCardV2`'s query functions returned `undefined` for a not-found id, which TanStack Query v5 treats as a bug ("Query data cannot be undefined") and logs to the console.** This was latent before (harmless as long as nothing routinely queried an id absent from a given store) but became a routine, reproducible console error once `CardEditEntry` started probing both `useCard` and `useCardV2` for every `cards/:id/edit` visit — a `CardV2Record`-native card is by definition "not found" in the v1 store, and vice versa for untouched legacy cards. Fixed at the source (`queryFn: async () => (await repo.X.getById(id)) ?? null`) in both hooks, not worked around in the one new call site, since `null` is the documented correct "not found" value and every existing consumer already used falsy (`!data`) checks that work identically with `null`.

**D71. Multiple Choice becomes the second complete vertical slice (chooser → editor → live preview → Deck row → Study preview → Review), following Recall's exact pattern.** Investigation going in found the scope much narrower than expected: the type (`McOption`/`MultipleChoiceInteraction` in `cardV2.ts`), the grading function (`domain/grading/multipleChoice.ts`), the registry entry and `MultipleChoiceView` (`reviewV2/interactions/multipleChoice/`), and the v1 `mcq` → v2 migrator (`migrateMcq`) were already built and tested from earlier Review-layer work — only the authoring/editor layer was missing. New files mirror Recall's exactly: `domain/cardsV2/multipleChoiceForm.ts` (form state, preview/record conversions, `legacyMcqCardToForm`, and a `validateMultipleChoiceForm` pure validator — ≥2 options, all filled, ≥1 correct, single-select forbids >1 correct), `domain/cardsV2/saveMultipleChoiceCard.ts` (same new/v2/v1-migrate save contract as `saveRecallCard.ts`), `features/cardsV2/MultipleChoiceOptionRow.tsx` (add/remove/reorder/mark-correct option row, reordering cloned from `reviewV2`'s `OrderingRow.tsx` move-up/down-button idiom so it stays keyboard-operable without dnd-kit), `MultipleChoiceFields.tsx`, `MultipleChoiceLivePreview.tsx` (adds an explicit "Reset preview" button + local remount-key counter, since unlike Recall's preset-driven reset, Multiple Choice's `ReviewSessionScreen` only returns to the unanswered phase on remount), and `MultipleChoiceEditorShell.tsx`. **Chose a parallel shell over extracting a shared `CardEditorShell`** despite `RecallEditorShell.tsx`'s own comment half-anticipating one ("other interaction types will get their own `*Fields`/`*LivePreview` pair behind the same shell shape") — duplicating the ~50-line header/Organize/layout block was judged lower-risk than modifying Recall's own file for a payoff that only matters once a third type's editor exists; revisit when Write Code (the next type) is built.

Two small, real defects were found and fixed in the already-built Review-time layer, not Recall: (1) `ReviewSessionScreen.tsx`'s shared `isInteractiveTarget` keyboard guard didn't exclude `role="radio"`/`role="checkbox"` elements, so a single Enter keypress on a focused Multiple Choice option could both toggle the selection and fire the shell's submit handler using a stale pre-toggle response in the same event — fixed by adding both roles to the guard's selector (this also happens to fix Walkthrough's embedded `StepMultipleChoice`, which shares the identical pattern). (2) `MultipleChoiceView.tsx`'s pre-submission selected-vs-unselected state was color-only (border/background tint, no shape or icon) — added a persistent leading indicator (filled circle for single-select, filled square for multiple-select, with a checkmark) so selection reads without relying on color, matching spec's explicit requirement. `CardRowV2.tsx`'s interaction-tile line and `CardStudyPreviewPage.tsx`'s type guard, which were both narrowly hardcoded to `'recall'` despite the components underneath already being fully generic, were generalized to read `card.interaction.type` directly — pure generalization, no new logic. Both `cardV2SearchableText` functions (Dexie, Supabase) gained a `multiple_choice` branch over option text.

## 2026-07-26 — Phase F continued: Write Code

The third complete vertical slice (chooser → editor → live preview → Deck row → Study preview → Review), following Recall's and Multiple Choice's pattern.

**D72. Investigation found the same shape of gap D71 found for Multiple Choice: the entire Review-time half of Write Code already existed from Phase E** — `WriteCodeInteraction` (`cardV2.ts`), the comparison function (`domain/grading/writeCode.ts`, already fully tested), the registry entry and `WriteCodeView` (`reviewV2/interactions/writeCode/`), the v1 `codeCompletion` → v2 migrator (`migrateCodeCompletion`), and a working `/design-preview/review/write-code` route (D27's decision — no partial read-only region, whole starter block editable, feedback swaps to the read-only viewer — was already implemented there and needed no changes). Only the authoring/editor layer was missing. New files mirror Multiple Choice's set: `domain/cardsV2/writeCodeForm.ts` (form state, preview/record conversions, `legacyWriteCodeCardToForm` via `migrateCard`, and a `validateWriteCodeForm` pure validator — prompt required, ≥1 accepted answer, every accepted answer non-blank; starter code is deliberately *not* required, since a blank starting point is a valid card), `domain/cardsV2/saveWriteCodeCard.ts` (same new/v2/v1-migrate save contract), `features/cardsV2/WriteCodeAnswerRow.tsx` (one accepted answer per `LazyCodeEditor` instance, with move-up/move-down/remove cloned from `MultipleChoiceOptionRow.tsx`'s idiom — rows are keyed by a form-only answer id, not index, so identity survives reorder/remove even though the persisted `WriteCodeInteraction.acceptedAnswers` is a plain `string[]`), `WriteCodeFields.tsx` (comparison's four toggles kept inside a collapsed `<details>` disclosure — "visually secondary" per spec, using the same custom `role="checkbox"` idiom as Multiple Choice's toggles), `WriteCodeLivePreview.tsx` (same resetKey-counter + "Reset preview" pattern as Multiple Choice's), and `WriteCodeEditorShell.tsx`.

**D73. `WriteCodeInteraction.editableRegion?` stays unconsumed — no author-facing control was added for it.** The task driving this slice asked for "editable region configuration" as an editor field, but D27 already established that `CodeEditor.tsx` has no mixed editable/read-only support and the whole starter block is the editable region; adding a region picker that doesn't affect Review behavior would be a control that lies about what it does. Instead, `WriteCodeFields.tsx` shows a short inline caption under the starter-code field documenting the current limitation, and the data model is untouched so a real per-region implementation (a genuinely new CodeMirror `changeFilter`/`Decoration`-based effort, not a config flag) can consume `editableRegion` later without a migration.

**D74. Chose a third parallel shell (`WriteCodeEditorShell.tsx`) over extracting a shared `CardEditorShell`, despite D71 flagging Write Code as the moment to revisit that call.** Asked explicitly; the answer was to keep duplicating the header/Organize/layout block rather than touch `RecallEditorShell.tsx`/`MultipleChoiceEditorShell.tsx` — those two stay the untouched reference implementations for this milestone. The extraction remains open, now with three near-identical shells as the evidence for when it's done.

No changes were needed to `CardRowV2.tsx`, `CardStudyPreviewPage.tsx`, the interaction registry, `WriteCodeView.tsx`, or `writeCode.ts` grading — all were already generic/complete for `write_code`. `CardTypeChooser.tsx`'s `ENABLED` gained `'write_code'`; `CardCreatePage.tsx`/`CardEditEntry.tsx` gained `write_code` branches (the latter also picks up v1 `codeCompletion` cards); both `cardV2SearchableText` functions (Dexie, Supabase) gained a `write_code` branch over accepted-answer text. No separate read-only Card detail screen was built for Write Code, consistent with D69 — `CardStudyPreviewPage` (which already shows language and starter code via `WriteCodeView` itself) plus the row's inline Edit/overflow menu is the intended "detail" surface for every interaction type, not just Recall/Multiple Choice.

## 2026-07-26 — Phase F continued: Ordering

The fourth complete vertical slice (chooser → editor → live preview → Deck row → Study preview → Review), following Recall's/Multiple Choice's/Write Code's pattern.

**D75. Investigation found the same shape of gap D72/D74 found for Write Code, one phase further along: the entire Review-time half of Ordering already existed from Phase E, shipped alongside Matching and Walkthrough** — `OrderingInteraction`/`OrderingItemV2` (`cardV2.ts`), the position-wise grading function (`domain/grading/ordering.ts`, partial-credit `score` already tested), the registry entry and `OrderingView`/`OrderingRow` (`reviewV2/interactions/ordering/`, 9 passing tests), and the v1 `ordering` → v2 migrator (`migrateOrdering`, which sets `randomize: true` unconditionally since v1 always presented shuffled). Only the authoring/editor layer was missing, exactly as Phase F's own status note already said. New files mirror Write Code's set: `domain/cardsV2/orderingForm.ts` (form state, preview/record conversions, `legacyOrderingCardToForm` via `migrateCard`, and a `validateOrderingForm` pure validator — prompt required, ≥2 items, every item non-blank), `domain/cardsV2/saveOrderingCard.ts` (same new/v2/v1-migrate save contract), `features/cardsV2/OrderingItemRow.tsx` (move-up/move-down/remove cloned from `MultipleChoiceOptionRow.tsx`'s idiom, minus the correct-toggle button — the item list's own order **is** the correct order here, so `correctOrder` is always derived as `items.map(i => i.id)` rather than authored via a separate control), `OrderingFields.tsx`, `OrderingLivePreview.tsx` (same resetKey-counter + "Reset preview" pattern as the other three), and `OrderingEditorShell.tsx` (a fourth parallel shell, same reasoning as D71/D74 — still no extraction).

**Also fixed, in the already-shipped Review-time `OrderingView.tsx`/`OrderingRow.tsx` (not part of the authoring gap, but a genuine pre-existing accessibility shortfall against spec):** moving an item via the Move up/down buttons or via drag now sets a local `announcement` string ("`<item text>` moved to position N of M") rendered in a visually-hidden `aria-live="polite"` region. Previously the only signal a move had happened was the visible index number and each button's `aria-label` text changing — neither is a reliable screen-reader announcement on its own. This is a small, additive fix (no visual/DOM/test changes to the existing 9 passing tests), not a reconsideration of D29's button-based keyboard approach, which stands as originally reasoned.

No changes were needed to `CardRowV2.tsx`, `CardStudyPreviewPage.tsx`, the interaction registry, `OrderingView.tsx`'s grading logic, or `ordering.ts` grading — all were already generic/complete for `ordering`. `CardTypeChooser.tsx`'s `ENABLED` gained `'ordering'`; `CardCreatePage.tsx`/`CardEditEntry.tsx` gained `ordering` branches (the latter also picks up legacy v1 `ordering` cards — note the v1 and v2 discriminants share the same string, unlike `mcq`→`multiple_choice`'s rename). No separate read-only Card detail screen was built, consistent with D69 — `CardStudyPreviewPage` (which already renders `OrderingView`'s own "Correct order" reveal) plus the row's inline Edit/overflow menu remains the intended "detail" surface for every interaction type.

## 2026-07-27 — Phase F continued: Matching

The fifth complete vertical slice (chooser → editor → live preview → Deck row → Study preview → Review), following Recall's/Multiple Choice's/Write Code's/Ordering's pattern.

**D76. Investigation found the same shape of gap D72/D74/D75 found for the previous three types, but one step richer: not just the Review-time half but the entire richer N-column/3-part/fixed-option data model, grading, and `MatchingView` already existed from Phase E** — `MatchingColumn`/`MatchingColumnItem`/`MatchingInteraction` (`cardV2.ts`, already deliberately beyond the spec's 2-column MVP per D13/D31), `gradeMatching`/`isMatchingResponseReady` (`domain/grading/matching.ts`, two-column/three-part/fixed-column cases already tested), the registered `matchingDefinition` + `MatchingView` (`reviewV2/interactions/matching/`, 5 passing tests covering keyboard pairing, clearing, taken-value unavailability, submit-gating, and feedback text), and the v1 `matching` → v2 migrator (`migrateMatching`, which already builds fixed vs. unique columns and an optional third column from `triple`/`options`). Only the authoring/editor layer was missing, exactly as Phase F's own status note already said.

New files mirror Ordering's set, generalized for a variable number of columns: `domain/cardsV2/matchingForm.ts` (`MatchingFormState` — a distinguished source-row list plus a variable `columns` list, each either free-text-per-row or a shared fixed option list; `toMatchingInteraction` synthesizes non-fixed-column item ids as `${rowId}:${columnId}`, deliberately reusing `migrateMatching`'s exact synthetic-id convention so a migrated legacy card round-trips losslessly; hydration reconstructs by id lookup against `relationships`, never by parsing synthetic id strings; `validateMatchingForm` checks column/row minimums, required cells, fixed-column option minimums, and duplicate-value detection for every place correctness depends on uniqueness; plus 8 exported pure mutation helpers — `addMatchingColumn`/`removeMatchingColumn`/`setColumnFixed`/`addMatchingOption`/`removeMatchingOption`/etc. — kept in the domain module rather than as local closures in the Fields component, unlike every prior type's convention, specifically so their cascade-cleanup invariants (removing a column strips it from every row's cells; removing or un-fixing a fixed option resets any row that referenced it) stay directly unit-testable), `domain/cardsV2/saveMatchingCard.ts` (same new/v2/v1-migrate save contract), `features/cardsV2/{MatchingOptionRow,MatchingColumnEditor,MatchingRow,MatchingFields,MatchingLivePreview,MatchingEditorShell}.tsx` (a fifth parallel shell, same reasoning as D71/D74/D75 — still no extraction).

**The "add a 2nd+ column" editor affordance is a generic "Add column" button, not a v1-editor-style "Three-part matching" checkbox** — confirmed with the product owner over mirroring the v1 `Editor.tsx`'s hardcoded left/right/third affordance, since a plain add/remove button generalizes to any number of columns without a special-cased triple toggle. Column reordering was deliberately left out of scope (add/remove only); row reordering was kept, since row order is genuinely user-visible (the display order of `MatchingView`'s accordion), unlike a fixed column's option order (no reorder control on `MatchingOptionRow`).

**No changes were made to the already-shipped `MatchingView.tsx`.** It renders as a single fluid accordion list at every viewport width — it never lays out columns side by side — so it already satisfies spec §20.2/§29.4's mobile requirement ("on mobile, use a stepwise pairing flow or **stacked assignment controls**") via that alternative, verified directly in a real browser at a 390px viewport alongside the desktop width. This is the one place this slice's own instructions explicitly allowed touching a completed Review component if a genuine shared problem turned up; none did.

No changes were needed to `CardRowV2.tsx`, `CardStudyPreviewPage.tsx`, the interaction registry, `MatchingView.tsx`'s grading logic, or `matching.ts` grading — all were already generic/complete for `matching`. `CardTypeChooser.tsx`'s `ENABLED` gained `'matching'` (only Walkthrough remains "Coming soon"); `CardCreatePage.tsx`/`CardEditEntry.tsx` gained `matching` branches (the latter also picks up legacy v1 `matching` cards, including `triple`/fixed-option ones — the v1 and v2 discriminants share the same string, like `ordering`'s). Verified end-to-end in a real running instance (dev server + a headless-browser driver), not just unit tests: two-column create/save/reload, a three-column fixed-option card create/save/reload, the Deck row's violet Matching tile, Study preview rendering and grading a real submission, and the narrow-viewport editor/preview tab layout — zero console errors at any step. No separate read-only Card detail screen was built, consistent with D69 — `CardStudyPreviewPage` plus the row's inline Edit/overflow menu remains the intended "detail" surface for every interaction type. As a pre-existing, unrelated gap noticed but out of scope for this slice: `cardV2SearchableText` (Dexie/Supabase) has branches for `recall`/`multiple_choice`/`write_code` but not `ordering` or `matching` — left as-is, since neither this slice nor Ordering's added one, and fixing it isn't part of the authoring-layer gap this phase targets.

## 2026-07-27 — Phase F continued: Walkthrough (sixth and final vertical slice)

**D78. Investigation found the same shape of gap D72/D74/D75/D76 found for the previous four types, confirming Phase F's own status note: the entire model, grading, Review UI, and migration for Walkthrough already existed from Phase E — only the authoring/editor layer was missing.** `WalkthroughInteraction`/`WalkthroughStep` (`cardV2.ts`, multi-range `focus` per D13), `gradeWalkthroughStep`/`objectiveStepIds`/`aggregateWalkthroughResult` (`domain/grading/walkthrough.ts`, already tested), the registered `walkthroughDefinition` + `WalkthroughView`/`StepResponse`/`state.ts`/`focusLines.ts` (`reviewV2/interactions/walkthrough/`, already tested), the `interactionTypeMeta.ts` entry (teal tile, `Grid2x2` icon), and the v1 `story` → v2 `walkthrough` migrator (`migrateStory`) were all already built. New files mirror Matching's set, generalized for a variable number of ordered steps: `domain/cardsV2/walkthroughForm.ts` (`WalkthroughStepFormState` — a flat, type-irrelevant-fields-ignored shape per step, mirroring `MatchingColumnFormState`'s `fixed`/`options` convention, rather than a discriminated union, so switching a step's response type never needs to reconstruct the step object; line ranges authored as `WalkthroughRangeFormState` add/remove rows of start/end numbers, not a free-text spec string; `validateWalkthroughForm` checks shared prompt/scenario, per-step response requirements matched to that step's type, and range well-formedness including against the shared code's actual line count; plus exported pure mutation helpers for steps/ranges/MC-options/accepted-answers, kept in the domain module rather than local Fields-component closures for the same cascade-testability reason `matchingForm.ts` gives, since steps nest one level deeper than any prior type's form), `domain/cardsV2/saveWalkthroughCard.ts` (same new/v2/v1-migrate save contract), `features/cardsV2/{WalkthroughStepEditor,WalkthroughFields,WalkthroughLivePreview,WalkthroughEditorShell}.tsx` (a sixth and final parallel shell, same reasoning as D71/D74/D75/D76 — still no extraction). Multiple Choice's step sub-editor reuses `MultipleChoiceOptionRow.tsx` directly (already generic over `McOptionFormState`, no adapter needed); exact-input's accepted-answer list is a small local row, not `WriteCodeAnswerRow.tsx`, which is code-editor-backed and wrong-shaped for plain-text answers.

**Two small, additive extensions were made to already-shipped Review-time code — the one place this slice's own instructions explicitly allowed touching a completed interaction, since Walkthrough is the first type with in-place multi-step navigation and genuinely exposes gaps nothing else needed:**
1. `ReviewSessionScreen.tsx` gained an optional `initialResponse?: InteractionResponse` prop (default `undefined`, used only as `useState`'s initial value) so `WalkthroughLivePreview` can seed a mid-sequence preview state and offer "Step N" inspection buttons that jump straight to any authored step (seeding earlier steps with neutral placeholder answers, author-inspection only — no objective result is attached, so no feedback banner appears on the seeded steps). Every other call site omits the prop; behavior for Recall/Multiple Choice/Write Code/Ordering/Matching is unchanged.
2. `WalkthroughView.tsx` gained explicit focus movement: the step panel (`tabIndex={-1}`) receives focus on `goTo` after the initial mount, so screen-reader/keyboard users land on the new step's prompt/highlight context instead of stale focus on the Previous/Continue button. Nothing else in `reviewV2` needed this — every other interaction type is single-screen.

**A real, minor defect was found and fixed during manual browser verification, not introduced deliberately:** `validateWalkthroughForm` could push the identical error string twice (e.g. two blank highlighted ranges on the same step both failing "Step N has an invalid line range."), which produced a React duplicate-key console warning against the error `<ul>`'s `key={message}` convention (shared by every prior type's Fields component). Fixed by deduping the returned `errors` array (`Array.from(new Set(errors))`) — `canSave` is computed from the pre-dedup array length, so no validation strength is lost, only the displayed list's uniqueness.

No changes were needed to `CardRowV2.tsx`'s existing structure, `CardStudyPreviewPage.tsx`, the interaction registry, `WalkthroughView.tsx`'s grading logic, or `walkthrough.ts` grading — all were already generic/complete for `walkthrough`, beyond the two additive extensions above. `CardTypeChooser.tsx`'s `ENABLED` gained `'walkthrough'` (all six types now enabled — Phase F's authoring milestone is complete); `CardCreatePage.tsx`/`CardEditEntry.tsx` gained `walkthrough` branches (the latter also picks up legacy v1 `story` cards via `legacyStoryCardToForm`). `CardRowV2.tsx` gained one new, Walkthrough-only affordance: a quiet `"N steps"` indicator next to the tags line — no prior type showed an item/pair/step count on the row, and this one is intentionally not retrofitted onto Ordering/Matching. No separate read-only Card detail screen was built, consistent with D69 (confirmed explicitly for this slice rather than assumed) — `CardStudyPreviewPage` (rendering the real `WalkthroughView`) plus the row's inline Edit/overflow menu remains the intended "detail" surface for every interaction type, Walkthrough included. Verified end-to-end in a real running instance (dev server + a headless-browser driver): a two-step Walkthrough (Recall step with a 3-range highlight spanning lines 2, 5, and 7–8, plus a Multiple Choice step) authored, live-previewed through the actual `WalkthroughView`, saved, round-tripped through the editor, shown correctly on the Deck row (teal tile, "2 steps"), and studied to completion (multi-range highlighting rendered correctly in Review, per-step feedback, final rating controls with the correct suggested rating) — zero console errors after the validation dedup fix above. As with Ordering/Matching, `cardV2SearchableText` (Dexie/Supabase) gained no `walkthrough` branch — the same pre-existing, unrelated gap, left as-is since it isn't part of the authoring-layer scope this phase targets.

## 2026-07-27 — Card create-flow UX pass: one continuous screen, sticky chooser, opt-in preview drawer

Product feedback on the Phase F create flow (all six types now shippable) targeted `CardCreatePage.tsx`/`CardTypeChooser.tsx`/the six `*EditorShell.tsx` files, not any interaction type's own authoring fields.

**D80. `CardCreatePage.tsx` no longer swaps its whole return value when a type is picked.** Previously selecting a type in "Choose interaction" replaced the entire page with that type's editor — a full-screen swap the chooser itself. Now the chooser always stays mounted; the matching `*EditorShell` renders in a section below it (keyed by `selectedType` so switching types remounts with a fresh form rather than carrying over stale state), revealed with the existing `.reveal-in` fade/slide-up animation (`src/index.css`, already used by Review/`PreviewPage`/`CardView` — reused rather than adding a second animation primitive). `CardTypeChooser.tsx` gained an optional `selected` prop so the active tile stays visibly highlighted (solid `itera-accent` fill) instead of losing that state once picked, since it's now a persistent control, not a one-shot chooser.

**D81. The chooser is `sticky top-[77px]` (not `position: fixed`), pinned directly beneath `AppShell`'s own already-sticky header (measured at 77px tall) rather than overlapping it.** Two sibling `sticky top-0` elements in the same scroll container don't auto-stack by height — each positions independently relative to the scrollport — so the offset is required. The wrapper carries `-mx-4 px-4 md:-mx-6 md:px-6` to cancel `<main>`'s own padding (`AppShell.tsx`) so its `bg-bg` background spans edge-to-edge behind it while scrolling, matching the header's own treatment, and a `z-[5]` (below the header's `z-10`) so it never visually competes with it. The 77px figure is specific to this route's active nav title/subtitle ("Decks" / "Organize decks and their cards") — if `findActiveItem` ever resolves a different (longer-wrapping) title for this route the offset would need re-measuring, but this is stable today for both `/decks/:deckId/cards/new`'s only entry point (the Decks page).

**Follow-up fix, same day: D80's first implementation still visibly jumped the chooser tiles when a type was picked, reported with before/after screenshots.** Root cause: the page-level "← Cancel" (shown only pre-selection, since `CardEditorShell` grows its own Cancel once a shell renders) was conditionally *unmounted* rather than hidden, and it lived inside the same sticky wrapper as the chooser — removing it shrank the content above the chooser within that wrapper, so the chooser's resting position (sticky elements sit at their normal in-flow position until actually scrolled past their `top` threshold — untrue-but-easy-to-assume that "sticky" alone prevents any reflow) shifted up by exactly the removed button's height. Pixel-measured against the reported screenshots: the chooser's leftmost accent-bordered pixel moved from `(700, 163)` to `(700, 125)` — a clean 38px vertical jump, no horizontal shift (the "moves to the side" in the original report was this same reflow, not a separate bug). Fixed by keeping the Cancel button always mounted at a constant height and toggling only `invisible` (`visibility: hidden`, not `display: none`) plus `tabIndex={-1}` once a type is picked, so the space it occupies never changes. Re-measured post-fix: identical `(700, 163)` bounding box before and after selecting a type.

**D82. Extracted `CardEditorShell.tsx`, reversing D71/D74/D75/D76/D78's repeated "keep duplicating, don't extract" call.** Those decisions each deferred extraction because the payoff didn't yet justify the risk of touching already-shipped, tested shells for one more near-identical copy. That calculus changes once a change must land *identically* in all six shells at once, which this request (opt-in preview drawer below) is — asked explicitly, and confirmed extraction now rather than a seventh hand-duplicated copy. `CardEditorShell` owns the header (Cancel / title / "Preview card" toggle / Save) and the wide-vs-narrow layout; each `*EditorShell.tsx` now only builds its `editorPane` (Fields + Organize block, unchanged) and passes it plus its `*LivePreview` element in. All six shell files shrank from ~135 lines to ~90, with no behavior change to their save/validate logic.

**D83. The live preview is now opt-in, not a permanent half-width column, on wide screens.** Previously `isWide` always rendered editor and preview as two `grid-cols-2` columns inside a `max-w-3xl` page (each column ~360px, too narrow for e.g. Matching's multi-column editor or Write Code's code editor). Now: the editor pane gets the page's full width (up to `AppShell`'s `<main>` cap of 1080px) and is itself centered at `max-w-2xl` for readable line lengths; a "Preview card" button in the header (only rendered when `isWide` — the narrow breakpoint keeps its pre-existing Editor/Preview tab toggle, untouched) reveals the preview in a drawer to the right. The narrow (<980px, `useIsWideEditor`) tab behavior is completely unchanged.

**D84. The drawer's open/close animation is a plain `width`/`opacity` CSS transition on an `overflow-hidden` wrapper around a fixed-width (`420px`) inner element, not a transform/translate.** This was deliberately chosen over `translateX` because animating the *box width* while clipping via `overflow-hidden` naturally produces "reveal grows left-to-right, hide retracts right-to-left" (more of the fixed-width content becomes visible from its left edge as the box widens, and vice versa) — exactly the direction asked for — with no JS measurement or transform math needed. The row's `gap` (0 ↔ 1.5rem) transitions in step via a sibling `.preview-shell-row` class so the editor pane doesn't jump when the drawer reaches zero width. Both rules live in `src/index.css` alongside a `prefers-reduced-motion: reduce` override (matching `.reveal-in`'s existing convention) that disables both transitions outright. Verified via a real running instance that no horizontal page overflow occurs mid-transition (`document.documentElement.scrollWidth` stayed pinned to the viewport width throughout), and that the narrow-viewport tab layout and all `cardsV2` component tests (36 passing, unaffected by the refactor since they only assert on the always-rendered editor tab/Save button) still pass.

## 2026-07-27 — Card create-flow UX pass continued: header realignment + a simplified preview

Product feedback (with before/after screenshots) on D80-D84's header: "Cancel / New card / Preview+Save" were three unrelated alignment anchors above a body whose width changed when the preview opened, and the live preview itself was cluttered with Review-session chrome irrelevant to authoring.

**D85. `CardEditorShell` now shares one literal outer width between its header and its editor/preview row, instead of each being independently laid out.** Both live inside one root div whose own `max-width` is `42rem` (`EDITOR_COL`) with the preview closed, or `calc(42rem + 1.5rem + 420px)` with it open (a `.card-editor-shell` CSS class transitions this, mirroring D84's drawer/gap transitions) — the editor pane no longer centers itself independently inside a wider shell (that self-centering was the actual bug: header spanned the full available width while the editor visually sat indented within it). The header is a `grid-cols-[minmax(0,1fr)_auto]` (stacking to one column below `sm:`) with a left-aligned title + contextual subtitle and a right-aligned action group — **"New card" → "Create card"**, subtitle is now `"{Interaction} · {Deck path}"` (a new `editorSubtitle()` helper in `shared/`, computed identically by all six shells from data they already have — `flatDecks`/`form.deckId`), **Cancel renamed "← Back to deck" and moved into the action group** (previously left-floating, now grouped with Preview/Save), and **Save is the only solid-orange action** (Preview/Back to deck are both neutral/ghost — already true for Preview, now true for Back to deck too).

**D86. Added `PageHeaderOverride` (`src/components/layout/`) so a focused-workspace route can replace `AppShell`'s topbar title/subtitle and hide its "Study now" CTA.** Product feedback: the topbar kept showing "Decks — Organize decks and their cards" plus a "Study now" button while actively creating/editing a card — contradictory context, and a second solid-orange CTA competing with Save. A `PageHeaderOverrideProvider` wraps `AppShell`'s layout (split into `AppShell` + `AppShellLayout` so the provider sits above both the topbar and `<Outlet>`); `CardEditorShell` calls `useSetPageHeader({ title, sub: subtitle, hideCta: true })` on mount, reverting to the nav-item default on unmount. Scoped to only fire once a shell is mounted (i.e., once an interaction type is picked) — the bare chooser screen still shows the default "Decks" header, judged an acceptable, low-traffic edge case rather than adding a second override call site in `CardCreatePage` for it. The context hook no-ops (doesn't throw) outside a provider specifically so the six `*EditorShell.test.tsx` files, which deliberately render a shell standalone without `AppShell`, keep working unmodified.

**D87. Replaced the editor's live preview's use of `ReviewSessionScreen` with a new, deliberately simpler `InteractionAnswerPreview` (`cardsV2/shared/`), for five of the six types.** Product feedback: "the only thing that should be shown is the flashcard itself... a toggleable show front/question and back/solution" — `ReviewSessionScreen`'s `ReviewTopBar` (Exit session / "N of N" / keyboard-shortcut hint) and `RatingControls` ("Preview only — nothing recorded") are real-session mechanics with no place in an authoring preview, and each interactive type's own "Submit answer" button implies an attempt-then-grade flow the preview doesn't need. Rather than growing `ReviewSessionScreen` itself with a pile of caller-specific hide-flags (its own doc comment already says it's "deliberately free of" concerns beyond the core Review shell), `InteractionAnswerPreview` renders the real `definition.View` — same production component, preserving D71's "not a fake mockup" fidelity — but drives `phase` from one local boolean via a plain Question/Answer segmented toggle instead of the reducer/grading/rating machinery. `InteractionViewProps` gained one optional field, `hideActions` (default undefined, set only by this component), which the four interactive views (`MultipleChoiceView`/`WriteCodeView`/`OrderingView`/`MatchingView`) use to (a) suppress their own "Submit answer" button and (b) force `locked` regardless of phase — the latter so an author idly clicking an option while inspecting the front can't leave the "Answer" toggle showing a misleading graded state; response is `undefined` for the entire preview's lifetime, so "Answer" always reflects a clean, consistent unanswered-attempt feedback view. This also let the four types' `*LivePreview.tsx` drop their old resetKey/"Reset preview" button entirely (nothing left to reset). Recall's `RecallView` needed no `hideActions` handling (no submit button; `onPrimaryAction` already just flips, now wired to toggle both ways). TipPanel/ExplanationPanel are kept (author-relevant authored content, not session chrome).

**Walkthrough was deliberately excluded from D87** and keeps using `ReviewSessionScreen` directly, since its existing bespoke "Step N" jump preview (`initialResponse`, added for Walkthrough specifically — see the Phase F Walkthrough entry above) doesn't fit a plain front/back binary — each step has its own answer, and the shell's `phase` only ever reaches `feedback` once, at the very last step. Instead, `ReviewSessionScreen` gained two small optional props, `hideTopBar`/`hideRating` (both default false — every other caller, including real Review, omits them and is unaffected), which `WalkthroughLivePreview` sets to drop the same session chrome without disturbing its step-navigation model. The rating-phase keyboard shortcut (`1`–`4`) is now also gated on `!hideRating`, since the buttons it triggers are hidden there — closes off a latent path where a stray keystroke in the Walkthrough preview could have called the real `reviewService.submit`.

All 343 tests pass (one single-run failure during verification was confirmed flaky — reproduced green on immediate re-run of both the isolated file and the full suite). Verified end-to-end in a real running instance: header/content edges pixel-aligned in both preview-open and preview-closed states; Recall and Multiple Choice previews checked with real authored content in both Question and Answer states; AppShell topbar correctly swaps to "Create card" / "{Interaction} · {Deck}" with "Study now" hidden once a type is picked.

## 2026-07-27 — App Shell and Visual Foundation Convergence

Card-interaction work was frozen for this milestone (Walkthrough was already the sixth and final type shipped in the entry above). The goal: remove the last legacy application frame — `AppShell.tsx`'s left `Sidebar`/`BottomNav`/per-route topbar — and converge every standard route onto one Itera top-nav shell, per the locked IA (`Today · Library · Progress`, Search/Create/Profile, Review immersive/chrome-free). This substantially completes Phase H's "global shell rewrite" and promotes its Library/Deck UI from fixture-driven preview to production, but **deliberately without Phase G's Collection/Deck entity split** — see D88 below for why, and what that means for Phase G/H's remaining scope.

**D88. Collections remain a UI-only derivation over the existing `Deck.parentId` tree, not a real `Collection` entity — Phase G (the Collection/Deck migration) was not run.** Phase H's original file list (`docs/itera-redesign-plan.md`) specified `src/domain/collections/tree.ts` operating on a real, migrated `Collection` type, and named Phase G's preflight report/explicit migration as a hard dependency. This milestone's own instructions forbade any Dexie/Supabase schema migration, so `src/features/library/collectionTree.ts` instead defines "Collection" purely structurally: any deck with at least one child deck is a Collection node in the Library's nav; childless decks are the actual browsable Library decks; a leaf deck's `collectionId` is simply its `parentId`. This is a real, provable invariant (every ancestor of a deck-with-children is itself a deck-with-children, so a collection's own `parentId` always resolves to another collection or is absent) — not an approximation — but it is not what Phase G's migration would produce: there is still no `Collection` type, no `collectionId` field, and a deck cannot hold both its own cards and children simultaneously in the derived Collection view (visiting it directly at `/decks/:id` still shows its own cards exactly as before — nothing is hidden — it just isn't offered as a Collection's "leaf deck row" in the browser list). Phase G's preflight report has still never been run against real data; the "decks with both children and directly-attached cards" ambiguous case Phase G's step 1 exists to measure is entirely unmeasured. **Phase G remains fully open** — if/when a real `Collection` entity is ever migrated in, `collectionTree.ts`'s derivation is the thing that gets replaced, not extended.

**D89. Route paths are unchanged; only the shell and the `/decks`/`/decks/:id` components moved.** `src/features/library/{LibraryBrowserPage,LibraryDeckPage}.tsx` (promoted from `design-preview/library-browser/LibraryBrowserPreviewPage.tsx` and `library-deck/LibraryDeckPreviewPage.tsx`, wired to real `useDecks`/`useSearchCards`/`useSearchCardsV2`/`useCreateDeck`/`useSaveDeck`/`useDeleteDeck` instead of `library-shared/fixtures.ts`) now render at `/decks` and `/decks/:id` in `src/app/router.tsx`, replacing `DecksPage`/`DeckDetailPage` — no new `/library` path was introduced, since ~15 existing call sites (`/review?deck=`, `/preview?deck=`, every "back to deck" redirect in `CardEditEntry.tsx`/`CardCreatePage.tsx`) already hardcode `/decks/:id` and a rename would be pure churn for a shell-convergence milestone. The nav *label* is "Library" (`src/components/layout/primaryNavLinks.ts`); the *path* stays `/decks`. `src/features/decks/{DecksPage,DeckDetailPage}.tsx` are **not deleted** — left present but unrouted, kept only until full feature parity (in particular `DeckDetailPage`'s dnd-kit drag-reorder and per-card suspend/move/delete, both already ported into the new `LibraryDeckPage` via the same `CardRow`/`SortableCardRow`/`CardRowV2` components DeckDetailPage used) is independently re-confirmed, per this milestone's "no destructive route cleanup" boundary.

**D90. `/review` moved from an `AppShell` child to its own top-level route in `router.tsx`, matching the same "structurally separate, not CSS-hidden" pattern `/` and `/design-preview/*` already used before this milestone.** Contrary to what earlier redesign-plan prose implied, `/review` was **not** already chrome-free in production before this change — it was a plain `AppShell` child, rendering inside the full sidebar/topbar exactly like Decks/Browse (verified directly by reading `router.tsx`, not assumed). `ReviewPage.tsx`'s loading/"nothing due" branches, which relied on `AppShell` for `IteraSurface`/colors, now wrap themselves in `IteraSurface` directly; `ReviewSessionV2`'s main branch already self-wrapped and needed no change. `RouteError.tsx` (the shared `errorElement`) is now also wrapped in `IteraSurface`, since an uncaught error previously rendered in the raw un-scoped palette (`index.html` still defaults `data-theme="dark"` pre-paint) — a 1-line strengthening of the existing dark-mode suppression, not new dark-mode work.

**D91. `AppShell.tsx` was rewritten in place** (`IteraSurface` → new `TopNav` → `<main>` → `Outlet`, no sidebar, no bottom nav) rather than added as a second, parallel shell — `Sidebar.tsx`, `BottomNav.tsx`, and `navItems.ts` (the sidebar/bottombar's single source of truth, including `findActiveItem`) were deleted outright, not deprecated in place, since they were pure chrome code with no persisted state (categorically different from the V1-types/Roadmap-data/backup-support deletions this milestone's instructions forbid). **`PageHeaderOverride.tsx` (D86) was also deleted, reversing D86** — the new shell has no per-route topbar title/subtitle slot at all for a route to override; `CardEditorShell`'s `useSetPageHeader` call (its only call site) was removed along with it, since that call existed purely to suppress the old topbar's "Decks"/"Study now" leakage, which no longer exists structurally. `TodayShell.tsx` (the Today-only top-nav shell from the "Aside" whole-app-visual-pass entry above) was also deleted — its JSX is now `TopNav` — and `TodayPage.tsx` lost its own wrapper accordingly, rendering through the shared `AppShell` like every other route. `design-preview/library-shared/LibraryTopNav.tsx` (a third near-duplicate copy of the same nav JSX) was deliberately **left untouched** — `design-preview/*` is explicitly out of scope for this milestone, so the duplication there is accepted, not fixed.

New shared primitives, all under `src/components/layout/`: `TopNav.tsx` (the nav bar itself, generalized from `TodayShell`'s JSX), `primaryNavLinks.ts` (`Today`/`Library`/`Progress` — Roadmaps excluded per the already-settled D11/D17, reached only via Deck Settings-style Deck Detail links now, hidden from primary nav), `CreateMenu.tsx` (new: a deck-picker popover for the global "+Create" action — the one place this milestone made a genuinely new UI-shape call, since nothing upstream specified it further; picking a deck, or "+ New deck", navigates to the existing, untouched `decks/:deckId/cards/new` route), `ProfileMenu.tsx` (new: avatar + Settings link + conditional sign-out, gated on `isSupabaseConfigured` exactly like `SettingsPage`'s existing Account section). `useNavBadges.ts` was kept and repointed: its due-count badge, previously keyed to a `/review` sidebar item that no longer exists in primary nav, now surfaces on the `Today` link instead (studying is reached from Today's hero / Library's per-deck "Study now", not a dedicated nav destination).

**D92. Font packages were swapped from static per-weight `@fontsource/{inter,inter-tight,jetbrains-mono}` to the variable `@fontsource-variable/*` builds**, per this session's mandatory font-bundling instructions (the static packages already satisfied "self-hosted, no CDN request," but the instructions specifically preferred variable builds). Confirmed the installed packages' actual emitted family names before wiring tokens (`'Inter Variable'`, `'Inter Tight Variable'`, `'JetBrains Mono Variable'`) rather than assuming them. `--font-itera-sans`/`--font-itera-mono` (`src/index.css`) now alias `--font-sans`/`--font-mono` instead of repeating an identical literal stack under a second name — one font stack per family, two Tailwind utility names pointing at it. `--radius-card` (12px, legacy) now aliases `--radius-itera-card` (14px) instead of the two coexisting as a real, undocumented value conflict — this one-line change is what actually fixes every legacy page's (Browse/Drafts/Stats/Settings/old Decks) card radius, since they all already use the `rounded-card` utility and needed zero per-file edits.

**Incidental, unrelated one-line fix found during the build-verification step, not part of card-interaction work:** `src/features/reviewV2/interactions/ordering/OrderingView.tsx`'s `locked` variable was `boolean | undefined` (via `hideActions`, D87's optional prop), which `tsc -b`'s production build (not `tsc --noEmit`, which doesn't catch it) rejected. Confirmed pre-existing via `git stash` against a clean checkout before touching it. Fixed with `Boolean(hideActions)` — a type-safety coercion only, no behavior change, since `hideActions` is only ever `true` or `undefined` in practice.

**No Dexie/Supabase schema changed. No V1 card types, the V1 registry, `ReviewSession.tsx` (confirmed orphaned — no importer besides its own `useReviewSession.ts`), Roadmap data/repo/Supabase table, or backup/import support were touched.** `docs/itera-redesign-plan.md`'s Phase H/I status blocks and the "Aside" section were updated to match (see there); this entry is the append-only record of why.

## 2026-07-27 — Flashcard redesign: all six interaction types now flip, shared icon/width system, TopNav trimmed

Product feedback (with two reference mockups, `recall-card.png`/`recall-card-revealed.png`, plus an inspo-icons set and an `itera-reveal-hint` motion reference) drove a pass across every `reviewV2` interaction view, `FlipCard`, and the shared Tip/Explanation/label chrome. Recall's own redesign (pill label, centered prompt, Tip/Explanation restyle) had already landed in an earlier pass this same day; this entry covers the broader follow-up.

**D93. `InteractionLabel` now resolves its icon and text from `INTERACTION_META` (`cardsV2/shared/interactionTypeMeta.ts`) by `type`, instead of each view passing its own literal `text`/`icon`.** This is the single source of truth "Choose interaction" already used, so the flashcard's pill and the type-picker tile can never drift apart. Icons were changed to match reference art from the inspo-icons set (browsed and confirmed pixel-by-pixel, not guessed): `Brain` (recall, was `HelpCircle`), `ListChecks` (multiple_choice, unchanged), `CodeXml` (write_code, was `Code2`), `ListOrdered` (ordering, was `ArrowUpDown`), `Link2` (matching, unchanged), `Waypoints` (walkthrough, was `Grid2x2`). The inspo-icons themselves (1024px glow/blur raster PNGs) were deliberately **not** used directly — confirmed with the user first — since they'd be an indistinct blur at the ~16px pill size and don't share a visual language with the existing small flat lucide icons; the raster art was source material for icon *choice*, not an asset to embed.

**D94. `CardTypeChooser.tsx`'s color treatment was fixed alongside D93**, per explicit product feedback that "every icon is orange... weakens selection hierarchy": unselected-but-enabled tiles now use a neutral border/icon (`border-itera-border`, icon `text-itera-navy`) with a navy hover state (`hover:border-itera-navy hover:bg-itera-navy-soft`); only the *selected* tile is orange (`border-itera-accent bg-itera-accent`, white icon) — previously every enabled tile (selected or not) rendered with an orange border and orange icon, so the selected state barely read as different from any other enabled option.

**D95. The flashcard column is now `max-w-2xl` (42rem), matching `CardEditorShell`'s `EDITOR_COL` exactly** — product feedback that the card was "too wide," with an explicit target ("the width of the Choose interaction part"). Implemented as a single wrapper inside `ReviewSessionScreen.tsx` around `definition.View`/Tip/Explanation/RatingControls, deliberately **not** wrapping `ReviewTopBar` — the reference mockups show the top bar (Exit session / N of N / shortcut hint) spanning the full session width edge-to-edge while only the card column beneath it narrows, confirmed by pixel-measuring the mockup rather than assumed.

**D96. All six interaction types now use `FlashcardSurface`/`FlipCard` (front = question, back = answer/results), not just Recall** — product feedback: "all cards should be flippable, that's the point of flashcards," including that clicking Submit/Check-answer should flip the card rather than appending feedback inline below the same face. `CardPanel.tsx` (the old shared non-flipping surface) is now used by nothing; kept in place, not deleted, since deleting a still-referenced-by-name shared component on a UI-only pass felt like unnecessary churn beyond what was asked. **Walkthrough was deliberately excluded** and still uses `CardPanel` — its multi-step model has no single front/back moment (each step grades itself inline via `StepResponse`; the shell's `phase` only ever reaches `feedback` once, at the very last step), so there is no natural "answer face" to flip to. This is a scoping call made without asking, flagged here for the user to revisit rather than forcing an awkward flip onto a type it doesn't fit.

For the four newly-flipping interactive types (Multiple Choice, Ordering, Matching, Write Code), each view now computes two separate content trees keyed off `flipped = phase.kind !== 'presenting'`: front renders the live interactive controls (options/drag list/accordion/code editor + Submit button) and returns `null` once flipped; back renders a read-only results view (graded option/row coloring, correct-order/expected-answer reveal, frozen submitted code) and returns `null` until flipped. The `null`-until-relevant gating isn't just tidiness — `FlipCard` always mounts both faces (only one is `aria-hidden` at a time), so without it, both the interactive and graded renderings of the same list existed simultaneously in the DOM, which surfaced as real test failures (`getAllByText` doesn't respect `aria-hidden`, so duplicate content was found in queries that don't scope by role) and would have doubled up interactive elements (a second live drag-and-drop list, a second live code editor) sitting inertly behind the visible face.

**D97. Multiple Choice's option rows no longer render a visible checkbox/radio glyph** — product feedback: "they should not actually display the checkboxes... the behavior is visible by their background color changing." `role="radio"`/`role="checkbox"` + `aria-checked` are retained on the row for accessibility; only the little square/circle indicator element was removed, selection is communicated by the row's own border/background color exactly as it already was for the *selected* state, just without the redundant glyph.

**D98. A real bug in `FlipCard.tsx` was found and fixed while wiring up the four interactive types, not present before this pass because Recall's faces never contained interactive children.** The face `<div>`'s own `onClick`/`onKeyDown` (the whole-card "click anywhere to flip" affordance) fired on any bubbled event, including from nested controls — pressing Space while typing in Write Code's code editor, or Enter on an Ordering row's move-up button, bubbled up and re-triggered the card's own flip handler mid-interaction (reproduced first as two failing tests: a premature flip while typing a space character, and a submitted-then-immediately-hidden Submit button). Fixed with `isOwnActivation`, which walks up from the actual event target to find the nearest interactive ancestor (same selector concept as the shell's existing `isInteractiveTarget` guard) and only proceeds if that nearest interactive element is the face itself, not a nested button/input/`.cm-editor`/etc. Confirmed the fix doesn't regress Recall (clicking its plain prose content still flips — `closest()` walks past the non-interactive text straight to the face, which does match, since it's the boundary itself, not a nested control).

**D99 (point 7 of the feedback). Both faces are now click/keyboard-activatable, not just the front.** Previously only the front face had `role="button"`/`onClick`/`onKeyDown`; once flipped, there was no way to flip back except a control outside the card entirely (`InteractionAnswerPreview`'s Question/Answer tab toggle). The back face now mirrors the front exactly (`role="button"`, `tabIndex`, the same `isOwnActivation`-guarded handlers). In a real `ReviewSessionScreen` session this is a no-op past the `presenting` phase (`primaryAction()` is one-directional and returns early once `phase.kind !== 'presenting'`, by design — you don't un-submit mid-grading), so the practical effect is scoped to where the original complaint was: `InteractionAnswerPreview`'s toggle-based `phase`, and the `/design-preview/review/*` pages, where clicking the revealed back now genuinely flips back to the question.

**D100 (point 8 of the feedback). Card-to-card transitions now play a CSS entrance animation instead of an instant remount-and-blink**, inspired by the two-overlapping-rotated-cards `itera-reveal-hint` reference: `.itera-card-enter`/`.itera-card-enter-active` (`src/index.css`, respecting `prefers-reduced-motion` like `.itera-flip*` already does) rotate/offset/scale/fade the new card in from a slightly-off-stack resting pose to flat/full-size/opaque over ~0.5s. This is implemented as a mount-only effect inside `ReviewSessionScreen` itself (one `requestAnimationFrame` flip from the CSS's resting state to `-active`), relying on the fact that every caller already remounts the whole component per card via `key={card.id}` — no new id-tracking needed. **This is a scoped-down version of the full ask**: a true two-card crossfade (the outgoing graded card visibly sliding/rotating away *while* the next one animates in from behind) was considered and rejected for this pass, since it would require keeping the outgoing `ReviewSessionScreen` instance mounted during the transition (its own `window` keydown listener, rating buttons, etc. still live) purely for a ~400ms visual, which is a meaningfully larger and riskier change than an entrance-only animation. Flagged here as a known gap, not silently scoped down.

**D101. `TopNav`'s Search affordance and the global `+Create` action (`CreateMenu.tsx`) were removed outright**, per explicit product feedback ("What am I searching? That should belong in Library. Create - as well."). `TopNav`'s `showSearch` prop was deleted (no remaining callers passed `false`); `CreateMenu.tsx` (introduced in D91 above, same day) is now fully deleted, not deprecated — it had exactly one call site (`AppShell.tsx`) and the file is dead the moment that's removed. `AppShell`'s `rightSlot` is now just `<ProfileMenu />`. No replacement search/create affordance was added to the Library page in this pass — the feedback named where these belong, not a spec for what they should look like there, so building them into Library is left as its own follow-up rather than guessed at here.

All 364 tests pass; `npx tsc --noEmit` and `npm run lint` are clean. Verified visually via a real headless-browser run against `/design-preview/review/recall` (front/back, code-block alignment) both before and after the RichText `text-left` fix (D-adjacent: `RichText.tsx`'s fenced-code-block wrapper gained an explicit `text-left`, since a centered ancestor's `text-align` was inherited into `LazyCodeView`'s code text otherwise — code must never center regardless of caller).

## 2026-07-27 — Library redesign: sidebar drills to decks, Deck page gets tabs/toolbar/pagination, top-nav streak

Product feedback (with a reference mockup, `library-use.png`) asked for a large set of Library/Deck-page enhancements in one pass — icons throughout, a Cards/Insights split, the card-list toolbar (search/type/status/sort) with pagination, a restyled deck-header action group, a top-nav streak indicator, and moving a "Tip" aside from a page footer into the sidebar's empty space. All of it landed in `src/features/library/*` and `src/components/layout/{AppShell,ProfileMenu}.tsx` plus a new `StreakBadge.tsx`.

**D102. The Collection sidebar tree now drills all the way down to individual decks, not just Collection nodes — a genuinely new capability, not a restyle.** Previously `CollectionNav.tsx` only rendered Collections (decks-with-children); a leaf deck was only ever reachable from the main content list. Confirmed via the mockup's zoomed sidebar (`sidebar_zoom.png`, pixel-inspected) that nested items use a small dot indicator (filled accent when active) rather than a distinct icon, while Collections get a `Folder` icon and "All Decks"/"Unfiled Decks" get `Layers`/`Inbox`. `NavDeck` (`CollectionNav.tsx`) gained `name`/`cardCount` fields; a new `DeckLeafRow` renders each leaf deck nested under its owning Collection (or under "Unfiled Decks", now independently expandable via its own chevron, previously just a flat scope-selecting button with no drill-down). Clicking a nested deck row navigates straight to `/decks/:id` (via `useNavigate` inside `CollectionNav`, matching `DeckRow.tsx`'s own convention) — a materially different action from clicking a Collection row (which only re-scopes the main content list, unchanged).

**D103. `LibraryDeckPage` now renders inside `LibraryShell` (the sidebar), which it never did before.** This was necessary for D102 to be visible from the Deck page at all (previously the Deck page had no sidebar whatsoever) and is also what makes the sidebar's own empty space — where the new Tip panel (below) now lives — exist in that context in the first place. Clicking a Collection label while on a Deck page navigates to `/decks?collection=...` (reusing the existing `selectionToSearchParams` round-trip already used by the breadcrumb) rather than filtering in place, since the Deck page has no "browse this collection's decks" content area of its own; the `selection` passed to `CollectionNav` here is an inert sentinel (`{kind:'collection', id:'__none__'}`) so no row falsely shows as "selected" — only the current deck's own leaf row highlights, via a new `activeDeckId` prop threaded through `CollectionNav`/`CollectionNavDrawer`/`LibraryShell`.

**D104. Fixed a real, pre-existing bug found while wiring `LibraryDeckPage`'s metrics for the sidebar's per-deck counts: the Deck page's own `metrics.dueCount` (the "due today" stat) was always 0.** `computeDeckMetrics(allCards, dueCards, v2CountByDeck)` was being called with a hardcoded `[]` for `dueCards` (unlike `LibraryBrowserPage`, which correctly passes real `useDueCards()` data) — `dueCount` is deliberately not derived from anything else, so this silently zeroed it. Added the same `useDueCards({now})` call `LibraryBrowserPage` already had. Confirmed via `git stash` that this predates this session's changes, not introduced by it.

**D105. The big deck-header mark now shows the owning Collection's name (e.g. "C++"), not the leaf deck's own name — resolving what first looked like a data-model gap.** The mockup's large square icon reads "C++" beside an h1 of "Declarations and Definitions" (a *different* string), which only makes sense as the parent Collection's mark, not `markLabelFor(deck.name)`. (`deck.language` was briefly considered as the source for this and rejected once actually read — `src/domain/decks/languages.ts` is a spoken/content language for card text, e.g. "English"/"Serbian", entirely unrelated to a subject like "C++"; using it here would have been a fabricated meaning for an existing field.) `markLabelFor` gained an optional `maxLen` parameter (default 2, unchanged for every existing small-mark call site) so the large header mark can pass `3` — enough for short acronym-like Collection names ("C++", "SQL", "STL") to show in full instead of being clipped to 2 characters.

**D106. The Deck page's action buttons were restructured to match the mockup's two-tier grouping** (a top row of quiet actions, a stacked primary/secondary pair below) rather than one wrapped row of four equal-weight buttons. "Flip through" moved into a new overflow (`⋯`) menu (`OverflowMenu`, already used by `DeckRow`/`CardRowV2`) alongside "Deck settings"; "Study Now" (solid, full-width of its column) and "Add Card" (secondary, same width) stack below it, in that order, matching the mockup. Cards/due/last-studied stats each gained a small leading icon (`Layers`/`CalendarClock`/`Clock`); mastery keeps its existing ring.

**D107. Added a Cards/Insights tab split to the Deck page — Insights is new, built entirely from data the Cards tab already computes, not a new metric.** Two small breakdowns (status: new/learning/review/relearning/suspended; type: per-interaction-type count across both v1 and v2 cards), each rendered with the existing `MeterBar` primitive. No new domain concept, no new query.

**D108. The Cards tab gained a combined search/type/status/sort toolbar and pagination (page size 10) spanning both v1 `Card` and `CardV2Record` cards in one list — the two are genuinely different types/components (`CardRow` vs `CardRowV2`), unified only at the metadata level.** A `RowMeta` shape (`id`, `kind: 'v1'|'v2'`, `title`, `typeLabel`, `state`, `suspended`, `due`) is computed for every card regardless of origin (`cardTypeMeta[type].label` for v1, `INTERACTION_META[type].label` for v2), and search/type/status/sort/pagination all operate on that combined array; rendering still dispatches to the real `CardRow`/`CardRowV2` per id via a lookup map. **Pagination and the existing v1 drag-to-reorder feature were resolved, not left in conflict**: dragging only ever needs to work in the default view (no search/filter, `Sort: Manual`), so pagination and sorting are only active (`paginating`) when a filter/search is set or a non-manual sort is chosen — in that default manual+unfiltered state, the page falls back to the exact pre-existing DnD code path unchanged, full list, no page-size cap. Status filter options are New/Learning/Review/Relearning/Suspended, plus "All" meaning "any non-suspended state" (not literally everything) — chosen so the toolbar's default (`All`) reproduces the old default-hidden-suspended behavior instead of silently changing it once the toolbar shipped.

**D109. Added `LibraryTip.tsx`, reusing the review flashcard Tip's visual language (icon square + header + text), placed in the sidebar below the Collection tree** — product feedback explicitly asked for this instead of a page-footer bar (which doesn't exist in the real app today; `library-use.png`'s footer tip was mockup-only). Content is one fixed, real, generally-useful hint (the actual Space/1–4 Review shortcuts, cross-checked against `ReviewSessionScreen`'s own shortcut-hint text) rather than the mockup's "View keyboard shortcuts" link, since no keyboard-shortcuts page/modal exists anywhere in this app to link to — inventing one was out of scope for a Library-page pass.

**D110. Added a day-streak indicator (`StreakBadge.tsx`) to the top nav, and a chevron to `ProfileMenu`'s avatar button — both are visual-only additions, one of them explicitly not backed by real data.** The streak number is a static illustrative "7", deliberately matching Today's `MomentumPanel` ("7-day streak," already commented there as placeholder — no day-over-day review-log streak aggregation exists anywhere in the domain layer). A real computation was considered (`useReviewLogs()` exists and fetches all logs) and rejected for this pass: it would mean a global, always-on, full-history query mounted on every route just for a decorative nav element, a poor cost/value trade for what product feedback described as wanting "the fire icon and day streak" visually. `ProfileMenu`'s trigger button gained a `ChevronDown` next to the existing avatar circle (still one button, same `aria-label="Profile"`, so no test needed to change for this part) to visually signal it opens a menu — it already did structurally (`aria-haspopup`/`aria-expanded`), the affordance was just not visible before.

Two tests needed updating as a direct, correct consequence of D102 (not a regression): `LibraryBrowserPage.test.tsx` asserted `findByText('Type Deduction')` (singular), which now legitimately matches twice (once in the sidebar tree, once in the main content row) — changed to `findAllByText(...)` + `toHaveLength(2)`, asserting the new duplication explicitly rather than loosening the check.

All 364 tests pass; `tsc -b` and `npm run lint` are clean. Verified end-to-end in a real running instance via a persistent-profile Playwright session (so seeded Dexie data survived across separate script invocations): created a nested Collection (C++ containing Fundamentals/Templates) and three cards across two interaction types, confirmed the sidebar tree/icons, the deck header (icon, stats, restructured actions, overflow menu), the Cards/Insights tabs, and the toolbar's sort transitioning cleanly from the default draggable view to the sorted/paginated one.

## 2026-07-28 — Library: opening a Collection (a Deck-with-children) now renders a matching identity view instead of the generic browser

Bug report: opening a leaf deck ("focused Deck page," `LibraryDeckPage.tsx`) matched the target visual system, but opening a top-level deck that has subdecks (e.g. "Arhitektura Računara") fell back to the plain, edge-to-edge `LibraryBrowserPage` composition — a bare "Library" title, a top-aligned `+ New Deck` button, and a flat admin-style deck table with column labels inside the bordered box — visually disconnected from the focused Deck page's centered identity-header/toolbar/list/footer rhythm. Root cause: `CollectionNav.tsx`'s Folder rows (`CollectionRow`) call `onSelect({kind:'collection', id})`, which only ever re-scopes `LibraryBrowserPage`'s existing "browse everything" composition — it never routed through anything that gave the selected Collection its own identity treatment.

**D111. Added `LibraryCollectionView.tsx`, rendered by `LibraryBrowserPage` when `selection.kind === 'collection'`, reusing the focused Deck page's shell primitives (`DeckMark`, the newly-extracted `shared/Stat.tsx`, `CardTableHeader`/`CardTableRowV1`/`CardTableRowV2`, `CardListFooter`, `RowFilterDropdown`, `DeckRow`/`DeckTableHeader`) instead of duplicating a second, near-identical composition.** The `selection.kind === 'all'`/`'unfiled'` root-browsing composition is unchanged in structure (still the plain "Library" page — there's no single selected entity to give an identity header to there), but its Sort control was also switched from a native `<select>` to `RowFilterDropdown` and its `DeckTableHeader` moved outside the bordered list box, for the same reason D108 already did this for the Cards table: column labels sitting inside the border read as a stray leftover, not deliberate. No Collection/Deck schema migration — this is presentation-only, exactly as before (`itera-decisions.md` D88); a "Collection" is still purely `Deck.parentId` derivation.

Specifics:
- **Identity header**: `DeckMark` (size `lg`, deliberately smaller than a leaf deck's `xl` — "modest," per feedback, and there's no single card-shaped entity being represented) showing the Collection's own initials, name, optional description, language badge, and four `Stat`s — child-deck count, total cards, total due, last studied (omitted entirely when there's no review history yet, not shown as "Never") — aggregated from the same per-deck `metrics` map `LibraryBrowserPage` already computes (`deckMetrics.ts` gained `aggregateMetrics`, a reduction over already-fetched data, not a new query). Mastery is deliberately not shown here — the spec driving this change explicitly listed only deck/card/due/last-studied as meaningful aggregate stats.
- **Actions**: an overflow menu (`Delete collection`, blocked with an alert if the Collection still has child decks or direct cards — mirrors the existing per-row deck-delete guard) plus a `Collection settings` toggle that opens `DeckSettings.tsx` unmodified (it already operates on a generic `Deck`, so it works verbatim for renaming/reparenting/language on a Collection node too). `+ New Deck` creates a deck with `parentId` set to the open Collection, nesting it directly — the root page's `+ New Deck` still creates a top-level deck, unchanged. No "Study Now" — the legacy scoped browser never had one either, and studying an entire Collection's subtree isn't a defined product behavior yet.
- **Decks section**: search/Due-only-filter/Sort toolbar (same visual language as the Deck page's Type/Status/Sort row) over the Collection's leaf-descendant decks (identical scoping logic to the pre-existing `getSubtreeCollectionIds` — intermediate sub-Collections are still flattened past, not shown as their own rows, exactly as the old composition already did; this was not a behavior this pass changed), paginated once a filter/search/non-manual sort is active (mirrors D108's `paginating` gate), rendered via the exact same `DeckRow`/`DeckTableHeader` components the root Library page uses.
- **Mixed legacy nodes** (a pre-Collection-model Deck that has both child Decks and cards of its own — never disallowed by the schema): surfaced as a second "Cards" section below Decks, using the same `CardTable*`/`CardListFooter` components the focused Deck page's Cards tab uses, labeled as legacy-compatibility rather than hidden. No pagination/search/sort was added to this sub-section (kept intentionally minimal — expected to be rare/nonexistent in practice) but suspend/delete/move mutations all work.
- **Deep-link consistency**: `LibraryDeckPage.tsx` (`/decks/:id`) now redirects (`<Navigate replace>`) to `/decks?collection=:id` whenever the resolved deck has children — closes the same bug for any entry point that still links straight to a deck id regardless of whether it's a leaf (`RoadmapCanvas.tsx`, old bookmarks), which previously rendered `LibraryDeckPage`'s "0 cards" leaf composition for a Collection instead of bouncing to the correct view.

Also extracted `LibraryDeckPage.tsx`'s local `Stat` and `collectionPath` into `shared/Stat.tsx` and `collectionTree.ts`'s exported `collectionPathFor`, and `LibraryBrowserPage.tsx`'s local `sortDecks`/`SortKey` into `shared/sortDecks.ts` (`DeckSortKey`), so both pages share one implementation instead of two copies drifting apart.

All 364 tests pass; `npx tsc --noEmit`, `tsc -b` (production build), and `npm run lint` are clean. Verified in a real running instance via headless Playwright: created a Parent Deck containing a Child Deck, confirmed the Collection view's header/stats/toolbar/list/footer match the focused Deck page's visual system, confirmed the leaf Deck page is unregressed, confirmed the delete-guard alert fires correctly for a non-empty Collection, and confirmed a direct `/decks/:id` navigation to the Collection's own id redirects to the Collection view with no console errors.

## 2026-07-28 — Progress page: `/progress` becomes the real Progress destination, replacing `/stats` in primary nav

Product brought a locked mockup (`progress.png`) for a new Progress page: a local sidebar (Overview/Decks/Activity/Review lag/Milestones/Achievements/Stats/Reports, grouped, plus an "Itera Pro" upsell), five KPI tiles, an activity heatmap, a retention-over-time chart, a deck-performance table, and a recent-milestones panel. An audit first established what's real: `ReviewLog` (via `repo.reviews`) is the only usable history source; `computeStats.ts` already had working streak/retention algorithms but nothing else in the mockup (sessions, accuracy, per-deck history, milestones) existed anywhere. Scope was resolved with the user before building (see below) rather than guessed.

**D112. `/progress` is a new route (`src/features/progress/ProgressPage.tsx`) added as a sibling of the existing `stats` child route in `router.tsx`; `primaryNavLinks.ts`'s "Progress" entry now points at `/progress` instead of `/stats`.** `StatsPage.tsx` and the `stats` route are completely untouched and stay mounted — a direct-URL safety net, not deleted, per the task's explicit "preserve `/stats` safely" constraint. `TopNav.test.tsx`'s active-link assertion was updated to render at `/progress` instead of `/stats` as a direct, correct consequence, not a regression.

**D113. Only "Overview" is a live sidebar destination; the other 7 mockup rows (Decks/Activity/Review lag/Milestones/Achievements/Stats/Reports) render as visibly disabled rows with a "Soon" pill, and the mockup's "Itera Pro" upsell card was omitted entirely.** Decided with the user: building the full sidebar composition/hierarchy (matching the locked mockup) without fabricating pages that have no real data source yet, and no paid tier exists anywhere in this product to honestly back an upgrade CTA. `ProgressNav.tsx` mirrors `CollectionNav.tsx`'s structure (grouped headers, a `Settings` link pinned to the bottom via `mt-auto`); `ProgressShell.tsx` mirrors `LibraryShell.tsx`'s local-sidebar-inside-workspace layout (own `useIsWideProgress.ts` hook, a deliberate copy of `useIsWideLibrary.ts` rather than a shared import, matching this codebase's existing feature-independence convention for library/design-preview).

**D114. New pure domain layer, `src/domain/stats/{dateRange,progressMetrics}.ts`, additive — `computeStats.ts` (legacy `/stats`) was not touched or refactored.** `dateRange.ts` has the preset/range/comparison-period/label-formatting logic; `progressMetrics.ts` has `clusterSessions`, `computeKpis`, `computeHeatmap`, `computeRetentionSeries`, `computeDeckPerformance`, and `deriveMilestones` — each pure and colocated with a `.test.ts`. Every function takes real `ReviewLog`/`Card`/`Deck` arrays; nothing is fabricated or hardcoded.

**D115. Two metrics needed new, non-obvious definitions since neither existed in the codebase before this page. "Total sessions" is reconstructed by gap-clustering `ReviewLog.reviewedAt` (>30min idle = new session)** — decided with the user as the approach, since no session boundary is persisted anywhere (`StudySession`/`ReviewEvent.sessionId` in `cardV2.ts` are unused spec types) and this task couldn't add schema. **"Avg. accuracy" is new: the "not Again" rate over *all* reviews in range (any card state), deliberately broader than "Retention rate," which keeps `computeStats.ts`'s existing definition (mature review/relearning-state reviews only) just re-parametrized to an arbitrary window instead of a fixed trailing 30 days** — accuracy reads higher than retention because early-stage (new/learning) reviews are easier to answer correctly than mature recall checks.

**D116. "Recent milestones" is entirely derived from `ReviewLog` history, not a persisted entity** — decided with the user. `deriveMilestones` scans for the first date three threshold families are crossed: cumulative review count (100/500/1000/2500/5000/10000), streak length (3/7/14/30/60/100 days), and trailing-30-day retention (70/80/90%). Session-count milestones were deliberately skipped — stacking a derived threshold on top of an already-heuristic session count wasn't worth the extra surface for this pass.

**D117. Every chart is hand-rolled SVG/CSS, matching this codebase's existing "no charting library" convention** (`PaceChart.tsx`, `MasteryRing.tsx`) — no charting dependency was added. `RetentionChart.tsx` follows `PaceChart.tsx`'s gradient-line-plus-dashed-baseline structure; `ActivityHeatmap.tsx` is a Mon–Sun/week-column grid with a 5-level intensity scale; `Sparkline.tsx` is a small shared stroked-polyline component used by both the Total Sessions KPI tile and the deck-performance table's trend cell. **Colors deliberately use Itera's existing locked navy/orange/success/warning tokens (`--itera-accent`, `--itera-success`, `--itera-warning`, `--itera-navy`), not the mockup's blue/purple hues** — the mockup was treated as the reference for composition/hierarchy/spacing/density/typography (as the task specified), not for introducing a second color system alongside the app's already-locked one.

No Dexie/Supabase schema changed, no new entities. `docs/itera-redesign-plan.md` and `docs/features.md` were updated in the same pass to reflect `/progress` as the real Progress destination.

All 387 tests pass (364 pre-existing + `TopNav.test.tsx`'s route fix, plus 23 new tests across `dateRange.test.ts`/`progressMetrics.test.ts`); `npx tsc --noEmit` and `npm run lint` are clean. Verified in a real running instance via headless Playwright with synthetic seed data (4 decks, ~1,080 review logs spanning 60 days, imported through Settings' existing JSON import): confirmed the empty-data state (no review history) before seeding, then the populated KPI row/heatmap (including the 7D/30D/3M/1Y toggle)/retention chart/deck-performance table/recent-milestones panel all render correctly with no console errors, and that `/stats` still renders unregressed at its old URL.

## 2026-08-11 — Matching review card: a connected two-column board (mockup `matching-card.png`)

Product brought a reference mockup (`itera-mockups/webapp/matching-card.png`): terms in a left column, values in a right column, each pair joined by a drawn line ending in a round badge — grey lines with a navy check while answering, success/error lines with a check or X once the answer is shown. Everything below is scoped to `src/features/reviewV2/interactions/matching/`; the response shape, `gradeMatching`, `isMatchingResponseReady`, and the card model are unchanged, so nothing about grading, scheduling, authoring, or persistence moved.

**D118. Matching now has two bodies, chosen by column count, behind one unchanged `MatchingView`.** A card with exactly one value column (the shape every legacy v1 non-`triple` matching card migrates to, and the mockup's case) renders `MatchingBoard.tsx`. A card with two or more value columns keeps the previous accordion, moved verbatim into `MatchingAccordion.tsx`. A board only reads as "connected" between two facing columns — three columns side by side would need lines from column 1 to column 3 running straight through column 2 — so the accordion stays as the honest 3+-column layout rather than being deleted or force-fit. `MatchingView.tsx` keeps the header, the flip surface, the submit button, and the result banner for both.

**D119. Connectors are measured geometry (an absolutely-positioned SVG of cubic paths), not a CSS or layout trick, because the right column is presented shuffled.** Its authored order mirrors the relationship rows, so presenting it unshuffled hands over the answer; that means a pair is usually *not* two facing neighbours and the line has to be drawn between two arbitrary rows. Positions come from an `offsetLeft`/`offsetTop` parent chain rather than `getBoundingClientRect`, deliberately: this renders inside `FlipCard`'s rotated container, where client rects are foreshortened mid-flip while offsets stay in layout space. Measurement re-runs on every render (bailing out via an equality check unless geometry actually moved) plus a `ResizeObserver`, so wrapping text, a revealed "Should be …" line, or a late font swap can't leave a line pointing at a stale position. Where there is no layout at all — the `happy-dom` test environment, where every offset reads 0 — no edges are produced and the board renders as two plain lists, which is why the component tests assert on accessible names rather than on drawn lines.

**D120. The badge sits at ~30% along its line (near the term end), not at the midpoint the mockup shows.** Every connector spans the same gutter, so two lines share a midpoint whenever their endpoint pairs sum to the same y — which is exactly what a crossing (rotated) set of pairings produces, and it stacked all three badges on one point in the first implementation (caught in a headless screenshot, not by a test). Anchoring near the term instead gives every row exactly one badge at its own height, which is also where the post-grading "Should be …" line appears. The mockup's centered look still holds whenever the lines happen to run straight across; this is a deliberate, documented deviation for the crossing case.

**D121. The right column's presentation order is owned by `MatchingView`, not by `MatchingBoard`.** `FlipCard` mounts front and back as separate positions in the React tree, so a board that shuffled internally shuffles *again* on reveal and shows the learner a different layout than the one they answered on (reproduced in a screenshot before the fix). `MatchingView` survives the flip, so the `useMemo`d order lives there and is passed to both faces. Fixed columns (a shared option list) are left in authored order — that order carries no answer information.

**D122. On the board, pairing works from either side and a claimed value can be taken, which is a deliberate divergence from the accordion.** Pick a term then a value, or a value then a term; re-picking a term's current value unpairs it; tapping a connected value with nothing selected detaches it. Assigning a value another term already holds *moves* it (the unique-column invariant still holds — the previous holder is cleared) instead of the accordion's "claimed values are disabled and marked (used)". The accordion disables them because there you cannot see what you would be taking; on the board the existing connection is drawn on screen, so a click that silently does nothing would be the worse behavior. Pairing state is still stated in text for assistive tech via each item's accessible name (`"const, paired with cannot be modified"` / `"…, not paired"`), and after grading every wrong row states its correct value in text under the term — the drawn line and badge shape carry the state visually, never hue alone.

**D123. The board is two columns at every width, which supersedes the mobile claim in the 2026-07-27 Matching-editor entry above** ("`MatchingView` renders as a single fluid accordion list at every viewport width — it never lays out columns side by side", offered there as this codebase's answer to spec §20.2/§29.4's "stepwise pairing flow or stacked assignment controls" on mobile). That remains true for 3+-column cards; a two-column card now does lay out side by side on a phone. Checked live at 390px rather than assumed: the columns hold, values wrap to two or three lines, and the connectors and badges still land correctly, so it is usable — but it is a narrower layout than the spec's mobile wording, and a width-triggered fall back to the accordion was deliberately **not** built, since it wasn't asked for and would swap the layout out from under a learner mid-answer. Flagged here as a known, revisitable gap.

`MatchingView.test.tsx` was rewritten around the board (keyboard-only pairing, value-first pairing, unpair, steal, submit gating, graded feedback) and keeps two tests for the accordion path against a three-column card. All 391 tests pass; `npx tsc --noEmit` and `npm run lint` are clean. Verified visually in a real headless-browser run (front, partially-answered, and graded faces) against a temporarily two-columned preview fixture — `/design-preview/review/matching`'s fixture itself is deliberately three-part (see the fixture's own comment) and was left alone, so that preview route still shows the accordion, not the board.

## 2026-08-11 — Matching, same day: the board generalizes to three columns, capped there, with shared many-to-one values

Follow-up product decisions on the entry directly above, taken after seeing it run. Two asks: don't leave 3-column cards on the old layout, and make sure a value can serve more than one term.

**D124. Supersedes D118's split.** There is no longer a per-shape body choice: `MatchingBoard` renders every Matching card, and `MatchingAccordion.tsx` (created in D118 an hour earlier, itself the old `MatchingView` body moved verbatim) is deleted rather than left unrouted — it was a fallback for a case that no longer falls back. A relationship across three columns is drawn as a **chain**: term → its value in column 2 → its value in column 3, one hop per gutter. That is what a relationship row literally is (a tuple across the columns), and it keeps one visual language for every Matching card. D118's reasoning for *not* doing this still stands for the layout it was rejecting — every column connecting back to the term can't be drawn, since those lines cross straight through the intervening column's cards — the chain sidesteps it by connecting each column to the previous one instead. A hop whose preceding column is unset starts from the term, so a half-filled row stays legible. Column labels render as headers once a card has more than two columns (they were the only thing the accordion showed that the board didn't); a two-column card still has none, matching the mockup.

**D125. Three columns is now a hard cap (`MAX_MATCHING_VALUE_COLUMNS = 2` value columns), not just a layout preference.** Product call: settle the shape at three and build everything around it. Enforced in the pure form layer (`addMatchingColumn` returns the form untouched at the cap, `validateMatchingForm` rejects anything over it so pre-cap data can't be saved forward), with the editor's "Add column" button replaced by a plain cap notice at three. Nothing existing is stranded: a legacy v1 `triple` matching card migrates to exactly three columns, which is the cap, not past it.

**D126. "Several terms connect to one value" is the existing `fixed` column, surfaced properly rather than modelled again.** A shared column already stores one option list for the whole card and grades by item id, so any number of terms may land on the same value and nothing is taken from anyone — the board draws that as converging lines, verified live with three terms sharing one value. What was missing was that this is *findable* and *legible*: the editor toggle now reads "One shared list of values (several terms can connect to the same value)" instead of "Pick from a fixed list (shared across rows)", and a value's accessible name lists **every** term connected to it (`"Yes, paired with std::vector, std::list"`), not just the first. Unshared columns keep one-to-one semantics, where assigning a claimed value moves it (D122).

**D127. With three columns, tapping a connected value means different things in the middle and the last column.** In the last column it detaches the pairing (D122's undo affordance, and the only one that doesn't require re-picking the term). In a middle column it is instead the anchor for the next hop, so it becomes the pending pick — which is how you fill column 3 without naming the term again. Found by a test, not by reasoning: the chain test failed because the middle value detached itself instead of continuing the chain. A shared column is never a detach target either way, since its value can belong to several terms and there'd be no saying which one to drop.

All 395 tests pass (`MatchingView.test.tsx` gained three-column tests for chain continuation, per-column accessible state, and one shared value serving several terms; `matchingForm.test.ts` and `MatchingFields.test.tsx` gained cap tests); `npx tsc --noEmit` and `npm run lint` are clean. Verified visually in a headless run of `/design-preview/review/matching`'s real three-column fixture — front and graded faces, including three lines converging on one shared value and a single wrong hop reading as a red X plus "O(1) random access?: Should be No" — and the two-column board re-checked afterwards for regressions. The mobile caveat in D123 applies more strongly at three columns.

## 2026-08-11 — Matching, third pass: /preview was rendering a second, untouched Matching renderer

Product report: "it's literally the same accordion style one." Investigated before changing anything, by importing a three-column matching card and screenshotting every screen that renders one. The board was fine on `/review`; the screen being looked at was `/preview`.

**D128. `/preview` (`src/features/preview/PreviewPage.tsx`) now renders the real v2 card — `migrateCard` + `ReviewSessionScreen` with `hideTopBar`/`hideRating` — instead of the v1 `CardView`/`getCardDefinition` registry.** This was the actual complaint: the app had *two* independent Matching renderers, and this page kept using `src/features/cards/renderers/matching/Question.tsx` (rows of `Choose…` dropdowns, old panel styling, green type badge) while `/review` used the redesigned board. Nothing about the redesign could ever show up here. The same is now true of all eight v1 types, not just Matching — which is the point: `/review` (`ReviewSessionV2`) and `cards/:id/study` (`CardStudyPreviewPage`) already migrate-on-read into the shared shell, and this page was the last one that didn't. Deleted along with the switch: the page's own `FlipCard` branch, "Check answer"/"Show answer" button, auto-result banner, and `RevealToggle` — the shell owns reveal, submit, grading, tip, and explanation. Kept local: the back link, the tag row, the "Card N of M" jump input, prev/next, and left/right arrow keys (reveal/rating shortcuts belong to the shell's own listener). The v1 renderers stay in the tree and are still used by `ReviewSession.tsx` (unrouted) and `CardEditorPage`'s own preview.

**D129. An interaction type can now ask for a wider card column: `InteractionDefinition.widthFor?(interaction) -> 'default' | 'wide'`.** Matching returns `'wide'` for a three-column board, everything else leaves it unset and keeps D95's 42rem/`max-w-2xl` flashcard column. Three tracks plus two gutters inside 42rem left each column too narrow to hold a line of text — "for 2 columns it's okay, but for three it's bad" — so at three columns the card takes `max-w-4xl` (56rem) and values stop wrapping three deep. Implemented as one optional registry field read by `ReviewSessionScreen`, not a Matching-specific branch in the shell, and not a change to the default width any other card sees. `PreviewPage`'s own column was widened to match so the wide variant isn't re-clamped by its container.

All 395 tests pass; `npx tsc --noEmit` and `npm run lint` are clean. Verified in a real browser against an imported three-column legacy v1 matching card: `/preview` now shows the board (wide, three columns on one line each, connectors and badges drawn), and the same card in `/review` is unchanged.

## 2026-08-11 — Library rows: preview-first click, portaled menus, and no more `window.*` dialogs

Three product reports against the Deck page's card table, all about the row rather than the card: clicking a row jumped straight into the editor, the kebab menu on lower rows opened somewhere far below the list (screenshot supplied), and destructive/creation actions used OS alert boxes.

**D112. Every popover in `OverflowMenu` and `CardTable`'s `MovePopover` now renders through a new `src/components/ui/FloatingPanel.tsx` — a portal into `document.body` positioned `fixed` against its trigger — instead of being an `absolute` child of the row.** The reported symptom had a specific cause: the card and deck tables sit inside an `overflow-x-auto` wrapper, and a box with `overflow-x: auto` computes `overflow-y: auto` too, so the menu was being clipped by that box and turning it into a vertical scroller rather than simply opening. Two more in-flow hazards were latent in the same place: a suspended row carries `opacity-55` and a dragging row a transform, both of which open a stacking context that a `z-20` inside the row cannot escape. Portaling sidesteps all three at once, and gives somewhere to put the behavior a fixed panel needs anyway: flip **above** the anchor when the panel does not fit below, clamp to 8px from every viewport edge, re-place on scroll (capture phase, so an ancestor scroller keeps the panel attached) and resize, close on outside `mousedown` or Escape. Verified in a real browser at 1400x800 (opens downward, fits) and forced into the flip case at 300px tall (anchor at y=230, panel drawn at y=88–226, i.e. above it).

`document.body` is outside `.itera-scope`, so each portal re-applies the `itera-scope` class on its own root and cancels that class's canvas `background` with an inline `background: transparent`. This is a real constraint on any future portal in this app, not a detail of these two: without the class the `itera-*` tokens resolve to nothing, and with the class but no override the overlay paints the page ground.

**D113. Clicking a card row opens the card in preview; Edit moved into the kebab menu.** Both row kinds previously linked to `/cards/:id/edit` from the title, with Preview demoted into the overflow menu (v1) or the row already opening the study preview (v2 — so the two row kinds actually disagreed with each other). Reading a card is the common case and it is non-destructive, so it is what a plain click should do; editing is now one deliberate step away. v1 rows go to `/preview?card=…`, v2 rows to `/cards/:id/study` — the two existing preview surfaces, unchanged. The `from` param is no longer the hardcoded `/decks` but the table's own current location (`useReturnTo()`), so the preview's back link returns to the deck or collection the user actually came from. This also settles `CardStudyPreviewPage`'s docstring claim ("the Deck-page row's primary click target"), which had stopped being true for v1 rows.

**D114. `window.confirm` / `window.prompt` / `window.alert` are replaced across the Library by a promise-based in-app dialog: `<DialogProvider>` (in `src/app/providers.tsx`) + `useDialogs()` in `src/components/ui/dialogs.tsx`.** The builtins are OS chrome — they ignore the design system entirely and, in an installed PWA, read as a browser security prompt rather than part of the app. The API is deliberately promise-shaped (`if (await dialogs.confirm({ title, description, danger }))`) so call sites stay as straight-line as the blocking builtins they replace; the only mechanical change at each site is `async`. Converted: `LibraryBrowserPage` (new deck / rename / delete / non-empty deck), `LibraryCollectionView` (the same four plus delete-collection), `LibraryDeckPage` (delete card, new deck), and `CardTable`'s v2 row (delete card). `Button` gained a `danger` variant (`bg-red`, which `.itera-scope` re-points to `--itera-error`) and `ComponentPropsWithRef` typing so the dialog can focus its confirm button.

Deliberately **not** converted in this pass: `SettingsPage`, `CardStateMigrationSection`, `RoadmapsPage`/`RoadmapEditorPage`, `CardEditorPage`, `BrowsePage`, and the unrouted `DecksPage`/`DeckDetailPage`. The provider is global, so those are a mechanical follow-up whenever each screen is next touched — the report was about the Library and widening it further would have buried the fix.

All 395 tests pass (`LibraryBrowserPage.test.tsx` now wraps its render in `DialogProvider`); `npx tsc --noEmit` and `npm run lint` are clean. Verified end to end in a headless browser against imported seed data: the last row's menu opens attached to its own trigger, row click lands on `/preview?card=…&from=%2Fdecks%2Ffund`, the menu's Edit lands on `/cards/:id/edit`, and delete/new-deck render the in-app dialog — with a Playwright `dialog` listener armed to fail loudly if any native `window.*` prompt still fired. None did.

## 2026-08-11 — Ordering review card redesigned to the `ordering-card.png` mockup

**D130. `OrderingRow` is now a card-shaped row: no leading position number on the front face, a grip on the right edge, and the Move up/down buttons hidden until hover or focus.** The mockup's resting row is label + grip and nothing else, but the up/down buttons are the *only* required reordering path (drag is additive — see the module comment in `OrderingRow.tsx` for why dnd-kit's `KeyboardSensor` was rejected), so they could not simply be deleted. They stay in the DOM, in tab order, and hit-testable at all times, and are faded with `opacity-0` (never `hidden`/`invisible`/`display:none`) — `group-hover` and `group-focus-within` bring them back, so a keyboard reaching a row reveals its controls. A boundary control (Move up on row 1) fades from 0 to 30% rather than sitting at 30% permanently, or it would be the one thing visible on an otherwise resting row.

The position number moved to the feedback face only. On the front it is redundant — the order *is* the answer, and it is already visible — but on the back it is what makes "Belongs at #3" readable against the row it labels. It stays a sibling of the label span, never a wrapper around it, so a row's visible text remains exactly the item's own text (the first attempt nested it and turned the row's text content into `"1.First"`, which `OrderingView.test.tsx`'s label assertions caught).

Also changed on this card, all presentational: rows go to `rounded-itera-card` with `px-5 py-4` and a 1px hairline shadow (from `rounded-itera-control`/`px-3 py-2.5`), row gap `space-y-2` → `space-y-3`, section gap `gap-4` → `gap-6`, the front subtitle takes the mockup's copy ("Drag items into the correct sequence.", `text-sm text-itera-muted-light`, replacing the smaller "Drag to reorder, or use the up/down controls." — the buttons still carry their own `aria-label`s), and Submit answer is centered with `px-8 py-3` instead of left-aligned. Graded rows now tint the whole border and fill (`border-itera-success/40 bg-itera-success-soft` / the error pair, matching `MatchingBoard`) instead of carrying a 4px left edge, which rendered as a crescent at the row's new 14px radius. Color is still never the only signal: every graded row also states "Correct" or "Belongs at #N".

**Untouched:** `gradeOrdering`, the response shape (`ID[]`), `isResponseReady`, the aria-live announcement, the card model, and `InteractionLabel`. The label is shared by all six types, and the mockups draw it orange (a soft-orange pill in `matching-card.png`, plain orange text here) rather than the current grey pill — deliberately left alone, since changing it is a six-card decision, not an Ordering one.

All 395 tests pass; `npx tsc --noEmit` and `npm run lint` are clean. Verified visually in a headless run of `/design-preview/review/ordering` — front, hover (one row's chevrons revealed), keyboard focus (focus ring on the control, boundary sibling at 30%), and the graded face.

## 2026-08-11 — Account menu: the avatar opens a real anchored popover, and `/settings` becomes the Account settings page (mockups `profile-menu.png`, `profile.png`)

**D131. `ProfileMenu.tsx` is replaced by `AccountMenu`/`AccountMenuContent`, and the popover is built on the existing `FloatingPanel` — no popover dependency (Radix, Floating UI, Headless UI) was added.** The old menu was a 48px-wide `absolute` child of its trigger with two rows, predating the App Shell convergence: wrong visual language, no menu keyboard semantics, no focus return, and structurally the exact shape D112 already established as clip-prone (an `absolute` panel cannot escape an ancestor's `overflow`/stacking context). `FloatingPanel` already does portal-to-body, `fixed` placement against the trigger's rect, flip-above-when-it-does-not-fit, an 8px viewport clamp, capture-phase re-placement on scroll, and outside-click/Escape dismissal — everything the task's positioning requirements ask for. Verified in a real browser at 1536x1000: panel width exactly 300px, panel right edge at 1408.0px against a trigger right edge of 1408.0px, 4px below the trigger, `z-index: 60`, and `main`'s bounding box byte-identical before and after opening (no layout shift, because the panel is portaled and `fixed`).

**D132. Focus management is an opt-in prop on `FloatingPanel` (`manageFocus`, `returnFocusTo`), default off, rather than logic inside `AccountMenu`.** Putting it in the primitive means the next menu that needs real keyboard semantics inherits them, and it avoids nesting a wrapper `div` between `role="menu"` and its `role="menuitem"` children purely to hang a key handler on. Default-off keeps `OverflowMenu` and `CardTable`'s `MovePopover` — pointer-driven menus shipped without it — behaving exactly as before.

**The focus-in effect is gated on the panel having been *measured*, not merely mounted, and this was a real bug caught only in a browser.** `FloatingPanel` renders `visibility: hidden` for the frame between mount and measurement (it has to lay out before its own size can be read), and `HTMLElement.focus()` on a `visibility: hidden` element is a silent no-op. The first implementation focused on mount and passed every `happy-dom` test — `happy-dom` has no visibility semantics — while in Chromium focus never left the trigger and no arrow key did anything. The effect now depends on `pos !== null`. Worth remembering: **component tests in this repo cannot catch focus bugs that depend on real layout or visibility**, so anything focus-related needs a browser pass.

**D133. The popover deliberately does NOT reproduce `profile-menu.png`'s full list** (which also has Study settings, Spaced repetition (FSRS), Import / Export, What's new). Confirmed with the user before implementing. It is a quick-navigation menu: Account settings · Preferences — Keyboard shortcuts · Help & documentation — About Itera — Sign out. Every settings *subpage* belongs inside the Account settings page, or the popover becomes a second settings sidebar that has to be kept in sync with the first.

**D134. Everything that does not exist yet renders as a focusable `aria-disabled` row with a "Soon" pill, not a `disabled` button and not a hidden row.** `disabled` removes an element from the focus order, so a keyboard user would never discover a row they can plainly see; `aria-disabled` keeps it reachable and announced as dimmed. The "Soon" pill exists because greying alone communicates the state only through color. Account settings is the single live row; Sign out activates only when `isSupabaseConfigured && session` (local mode has no session to end), and it uses the existing `useAuth().signOut` — no new auth path. Sign out is styled with restrained orange (`text-itera-accent`), not the red `--itera-error` this design system reserves for destructive Delete actions, matching how the mockup draws it.

**D135. `/settings` is now `AccountSettingsPage` with its own `/settings/:section` sub-navigation; the pre-Itera `SettingsPage.tsx` was deleted rather than left unrouted.** Unlike `DecksPage.tsx` (kept pending parity re-confirmation), nothing was left behind to verify: its content moved wholesale. Export/Import JSON became `sections/ImportExportSection.tsx` (verbatim, except the Replace confirmation now goes through `useDialogs` per D114 instead of `window.confirm`), and the Phase D migration panel became `sections/CardSchedulingSection.tsx` wrapping the untouched `CardStateMigrationSection`. **These two sections stay fully functional** — the user's call, and the right one: greying out a working backup/restore path to match a mockup would be a regression. Every other section (Profile, Email & password, Appearance, Notifications, Privacy, Connected devices) is an inert placeholder that says what it will hold and why it is empty.

**D136. Billing and Plan & usage, both present in `profile.png`, were dropped entirely rather than added as placeholders.** There is no billing, subscription, or usage-quota concept anywhere in this product and none is planned; a settings page should not advertise one. Every remaining nav row is at least a real intended destination.

**D137. Statistics on the Profile section show em dashes, not numbers.** Real values exist (`/progress` computes them from `ReviewLog`), but wiring them into a section whose surrounding form is inert would make the page ambiguous about what works. The caption points at `/progress` instead.

**Identity is a greyed placeholder in local mode** ("Your name" / "Not signed in"), filling in the real Supabase email once a session exists — rather than hardcoding the mockup's sample person, which would put fabricated personal data in the product.

**Responsive:** below 480px (`useIsNarrowShell`, matching `useIsWideLibrary`'s shape) the identical `AccountMenuContent` renders in a bottom sheet, following `CollectionNavDrawer`'s overlay pattern turned to the bottom edge. Dismissed by the same rules as the popover; explicitly not a persistent mobile sidebar.

**Untouched:** `/review` (a top-level route with no `AppShell` ancestor, so it has no account menu by construction — re-verified: 0 account triggers, 0 nav links on the page), `AuthProvider`, `CardStateMigrationSection`, the backup domain layer, `OverflowMenu`, `CardTable`, and every `design-preview/*` route.

406 tests pass (395 before, +11 new across `AccountMenu.test.tsx` and `AccountSettingsPage.test.tsx`); `npx tsc --noEmit` and `npm run lint` are clean. Browser-verified in Chromium: open/close, outside click, Escape, route-change close, Enter and Space to open, ArrowDown/ArrowUp/Home/End across items, focus return to the trigger on dismissal, the 390x844 bottom sheet, and the Account settings page after navigation.

## 2026-08-12 — `/login` exists, and the app has a real session boundary (mockup `login-v3.png`)

**D138. Local mode is gated now.** Confirmed with the user before implementing. Until this entry, `AuthGate` only ever rendered a login screen when `isSupabaseConfigured`; the default local-first mode had no session, no sign-out, and no `/login` route, which is why `AccountMenuContent`'s Sign out row was permanently greyed (D134). A fresh browser now lands on `/login` and must sign in or continue with the demo workspace. **No data is touched** by this — IndexedDB is where it always was; only reachability changed, so existing installs see the login screen once.

**D139. `src/auth/localSession.ts` is the single storage seam, and nothing else may open one.** One key (`itera.session`), `localStorage` when Remember me is checked and `sessionStorage` otherwise, every access in `try/catch` because storage throws outright in some privacy modes, and a corrupt value reads as signed out rather than crashing the shell. A write always clears the other store first: `readLocalSession` prefers `localStorage`, so without that a stale remembered session would silently shadow a newly-created un-remembered one (found while writing the test, not in review). The point of concentrating this is that "am I signed in?" has exactly one answer to change when a real local account system arrives.

**D140. The guard is one pathless layout route, not a per-page check.** `RequireAuth` (`src/auth/RequireAuth.tsx`) wraps the whole `AppShell` tree *and* `/review` in `router.tsx`; `/login` and `design-preview/*` are outside it — design-preview deliberately, since it is dev fixtures with no product data. `AuthGate` was narrowed to what it is actually needed for (blocking on the Supabase session bootstrap so a signed-in user is not bounced to `/login` on a refresh) and no longer decides what renders, because it wraps `RouterProvider` and therefore cannot redirect. The requested route is carried in `location.state.from`, so signing in returns there rather than always dumping the user on Today.

**D141. Supabase mode keeps magic-link; no password authentication was added.** Confirmed with the user. When `isSupabaseConfigured`, the password field, Remember me and the demo divider are hidden, the button reads "Send magic link", and success swaps in the check-your-email panel — the previous `LoginPage`'s behavior, moved into `SignInPanel`. The alternative considered and rejected was `signInWithPassword`: the visual design would be identical in both modes, but this project's Supabase users were created OTP-only, so it would have shipped a button that cannot work. `src/auth/LoginPage.tsx` was deleted outright (unlike `DecksPage.tsx`, kept pending parity) because its content moved wholesale.

**In local mode the password is a development shell and the code says so.** It is never stored, transmitted, or checked against anything; the only validation is "present, and the email is well-formed". This is stated in `SignInPanel.tsx`'s header rather than left for a reader to infer from the absence of a network call. Forgot password is a focusable `aria-disabled` button that reveals an explanatory line — the D134 honest-placeholder pattern — not a link to a route that does not exist, and no reset backend was built.

**D142. `AuthValue` grew rather than changed shape.** `session` still means "the Supabase session" and still returns `null` in local mode, so every existing consumer kept compiling; `isAuthenticated` and `identity` are the new things to read. A real Supabase session outranks a local one when both exist, since the cloud identity is whose data the app is actually reading. Account settings shows "Demo workspace — everything is stored in this browser." rather than the demo's synthetic `demo@itera.local`, which would read as a real account.

**D143. The illustration's overlap is constrained, not styled.** `LearningCardsIllustration` is three absolutely-positioned CSS cards, each with one literal `style.transform` rotation (the CLAUDE.md composed-transform gotcha, and it keeps the geometry readable in one place). The rule the numbers encode: **a card may never cover the next card's title.** That is tighter than the `left` values suggest, because a leaning card's right edge at its neighbour's title height sits further right than its own box by `sin(rotation) x` the vertical distance from its center — the first two passes both clipped "SQL Joins" and then "System Design" this way, visible only in a browser screenshot. Anyone widening a card or nudging one left has to re-check that in Chromium, not in the numbers.

**Deliberate deviations from the brief where it conflicted with the locked mockup**, all resolved toward the mockup: the panel heading is left-aligned (the brief said centered); the three principles are icon + label only, with no descriptive sub-copy (user's call); the principle icons are navy linework with no orange accent. Going the other way, the demo button carries a small `FlaskConical` icon the mockup does not draw, because the brief asked for one explicitly. The email placeholder is the mockup's `you@example.com`, not the brief's sample personal address. Sign-in redirects to `/`, which is this app's actual Today route; there is no `/today` path.

**Responsive:** below `lg` the two columns stack (brand block, then form) and the illustration drops out; below `md` the principles row drops out too. The form is not hoisted above the brand block on mobile — the logo and headline are compact, and a login page whose first pixel is a bare form field with the mark below it reads worse than the alternative. Verified at 390x844: no horizontal overflow.

447 tests pass (428 before, +19 across `localSession.test.ts`, `RequireAuth.test.tsx` and `LoginPage.test.tsx`); `npx tsc --noEmit`, `npm run lint` and `npm run build` are clean. Browser-verified in Chromium at 1440x900: `/`, `/decks` and `/review` all redirect to `/login` while signed out; `/design-preview` does not; demo workspace and credential sign-in both reach the app (the latter returning to the originally-requested `/decks`); `/login` while signed in redirects to Today; the account menu's Sign out is enabled and returns to `/login`, and a reload stays signed out; Remember me lands the session in `localStorage`; tab order runs email -> password -> show/hide -> Remember me -> Forgot password -> Sign in -> demo, the eye toggle has a 2px `#FF6902` focus ring and operates from the keyboard, and Enter submits.

## 2026-08-12 — Documentation reconciliation: an agent-neutral doc hierarchy, and product truth moved out of the Claude master spec

Context: the repository is now worked on by both Claude Code and OpenAI Codex. No product implementation work was done in this pass; only documentation, three archived files, and doc-path references inside source comments changed.

**D144. Product truth no longer lives in an agent-specific file.** `itera-claude-master-spec.md` was the Claude-addressed master specification, and it still held the only copy of a large amount of agent-neutral truth: the locked logo rules, the orange rule's full statement, typography rules, shape language, page widths, top-bar heights, the global-navigation blueprint, the Library sidebar and deck-row rules, the Review-shell "never add" list, the motion language and timing table, the accessibility checklist and Review shortcuts, and the responsive matrix. All of it was consolidated into `design-system.md` (visual/interaction), `architecture.md` (ontology, local-first, layering, scheduler boundary) and `features.md` (Tip vs Explanation, grading model, study sessions, deferred scope) before the spec was archived. The spec itself is kept **whole and unedited** under `docs/archive/` because `itera-decisions.md` cites it by section number throughout; editing it would break those citations and violate the append-only rule indirectly.

**D145. `CURRENT_STATE.md` is the entry point, and it is agent-neutral by construction.** Moved from `docs/itera/CURRENT_STATE.md` to `docs/CURRENT_STATE.md`. It owns implementation status, known problems and the single recommended next milestone; the reference docs own mechanism, rules and rationale respectively. The rule that follows: `architecture.md` describes how the system is structured, not what shipped last week, and status commentary belongs in `CURRENT_STATE.md`.

**D146. The redesign plan is history, not a plan.** `itera-redesign-plan.md` was archived. Reality had already deviated from its A-M sequence in ways the file itself records with strikethroughs (Phase H shipped without Phase G; Today shipped ahead of Phase I; Progress shipped without touching `computeStats.ts`), and keeping it labelled "authoritative, continuously updated" in the index meant two documents claimed to define what happens next. The still-open forward work it described is preserved: Phase G in `itera-migration-plan.md` sections 0 and 6, Phase D's remaining steps in section 4, and Phases K/L/M as the "Planned" rows in `features.md`. **The next milestone is now defined in exactly one place**, `CURRENT_STATE.md` section 17.

**D147. Archived documents may never override canonical ones.** Each archived file opens with a visible HISTORICAL warning naming what supersedes it. `docs/README.md` states the source-of-truth order explicitly (repository implementation, then `CURRENT_STATE.md`, then the canonical doc that owns the topic, then archive), and both `CLAUDE.md` and `AGENTS.md` repeat it. Nothing was deleted in this pass.

**D148. No `docs/references/` directory was created.** The target structure suggested one, but no visual reference is tracked in git - the mockups live at `C:\Users\SK\Desktop\itera-mockups\`. Creating the directory would have meant inventing paths that resolve to nothing. Instead `design-system.md` section 14 documents the external location and introduces three explicit tiers for how a reference is treated: **LOCKED** (authoritative for composition, hierarchy, spacing, density, typography), **DIRECTION** (feel only, never structure) and **CONCEPT** (history only, never implement from one). The two standing exceptions were already settled and are restated there: colors always come from the Itera token palette, and a mockup element with no backing feature is omitted or rendered as an `aria-disabled` "Soon" row.

**D149. `ai-card-prompt.md` moved to `docs/prompts/` rather than being folded into a product doc.** It is an active, user-facing tool referenced from the root `README.md`, and it describes the v1 8-type backup schema - which is still correct, since `BACKUP_VERSION` is still `1`. It will need updating when that bumps.

**Corrections made against repository evidence**, each verified in the working tree rather than inferred: `AccountMenu`'s Sign out is live whenever any session exists, not only a Supabase one (`architecture.md` still said the latter); a `CardV2`-backed store does exist, so the claim that "v2's storage model isn't live" was wrong - the accurate statement is that v2 records are absent from the **due queue**; `CardRowV2` is rendered only by the unrouted `DeckDetailPage`, not by production's deck page, which uses `CardTable`; `useCardsV2` exports six `useSave*Card` hooks, not two; the migration plan's claim that "no migration files exist yet" was false (`supabase/migrations/0001_card_states.sql`), and the same check surfaced a genuine gap - **`cards_v2` is declared in `schema.sql` with no matching migration file**, so a project migrated file-by-file would not have it.

**One measured contradiction left standing.** The 2026-08-12 login entry above records "447 tests pass"; a measured `npx vitest run` on this tree reports **63 files, 429 tests**. The decision log is append-only, so that entry was not edited; `CURRENT_STATE.md` section 16 now states the measured number and says explicitly that it supersedes the logged one.

Verification for this pass: `npx vitest run` 429/429 passing, `npx tsc --noEmit` clean, `npm run lint` clean except the pre-existing `.scratch-shot.cjs` warning. Only doc-path strings inside comments changed in `src/` (`router.tsx`, `reviewService.ts`, `PreviewShell.tsx`, `fixtures.ts`, `TodayPage.tsx`, `index.css`, `types/cardV2.ts`, `types/index.ts`); no behavior, routes, schema, persistence or UI changed.

## 2026-08-15 — Login visual refinement (`login-v3.png` + `login-icons.png`)

**D150 (supersedes the principle-row styling recorded under D143). The login keeps the approved composition but lowers its visual volume.** The desktop shell is now capped at 1080px instead of 1180px; outer and inner padding, the logo, headings, inputs and buttons were reduced proportionally. The three-card illustration is uniformly scaled to 90% rather than having its hand-tuned card positions edited individually, so D143's title-overlap constraint remains intact. Typography continues to use the existing self-hosted Itera families: Inter Tight for the expressive brand and sign-in headings, Inter for UI and supporting copy.

**`login-icons.png` is the locked treatment for the bottom principles.** The former bordered-circle icon-and-label row is replaced by three open columns with navy Lucide line icons, small orange check details where the reference uses them, labels, supporting copy and restrained vertical separators. This deliberately reverses the earlier call in D143 to omit descriptions and orange accents; the product owner explicitly requested the newer `login-icons.png` treatment. No raster icons or new icon package were added.

**Auth and responsive behavior are unchanged.** Local sessions, Remember me storage selection, demo entry, redirect-back behavior, Supabase magic links, password visibility, validation and the honest Forgot password placeholder all retain their existing implementations. Below `lg` the CSS-card illustration remains hidden, and below `md` the principle row remains hidden; at 390x844 the compact brand introduction and full form fit without horizontal overflow.

Verification: `npx vitest run` 429/429 passing, `npx tsc --noEmit` clean, `npm run lint` clean, and `npm run build` successful. Chromium at 1440x900 measured the shell at 1080x695 and preserved the complete keyboard order; Chromium at 390x844 measured 390px client/scroll width and no page height beyond the viewport.

## 2026-08-15 — Login card fan and typography correction

**D151 (supersedes D150's illustration scale and login-heading font). The CSS illustration returns to its full 580x330 authored size and matches the locked fan direction.** Dynamic Programming remains at −9°; SQL Joins changes from −5° to +5°; System Design changes from −7° to +9°, the direct right-leaning counterpart to the front card. Stronger layered shadows and a restrained card edge add depth without replacing the accessible, maintainable DOM illustration with a raster asset. Card titles, body copy and tags are larger in the rendered result; the long Dynamic Programming title remains one line.

**The login page uses Inter Variable throughout.** `LoginPage` scopes `font-itera-sans` over the whole surface, removes `font-itera-display` from the brand, `Welcome back` and `Sign in`, and sets the two headings at weight 650. Body copy remains 400; labels and actions stay mainly 500–600. Letter spacing and line height were retuned for the softer optical density of `login-v3.png`. This is deliberately page-scoped: Inter Tight and JetBrains Mono remain installed and unchanged for their sanctioned uses elsewhere.

The 1080px outer shell, `login-icons.png` principle row, auth behavior and responsive collapse rules from D150 remain in force.

Verification: Chromium computed both headings as `Inter Variable` at weight 650 and the three card transforms as `rotate(-9deg)`, `rotate(5deg)` and `rotate(9deg)`; the long front-card title stays on one line. Chromium at 1440x900 and 390x844 reported no horizontal overflow, with Email still receiving the first keyboard focus and the demo action's hover state exercised. `npx vitest run` passed 429/429, `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed successfully.

## 2026-08-15 — Equal-width card fan and controlled login font comparison

**D152. All three illustration cards now share Dynamic Programming's 176px authored width.** Their heights remain slightly different, and their vertical positions deliberately do not align: Dynamic Programming stays at top 62, SQL Joins moves from 26 to 40, and System Design moves from 16 to 30. System Design also shifts right to left 416 so the wider SQL card never covers its title. The existing −9°/+5°/+9° fan, layer order and full-size 580x330 illustration stay intact.

**The typography comparison is a temporary, family-only experiment on desktop `/login`.** A defaults to Inter, B selects Manrope, and C selects Plus Jakarta Sans. One inherited `fontFamily` on the login composition is the only value that changes: component classes continue to own the exact same sizes, weights, tracking and line heights for every option. The selector is not persisted and is hidden below `lg`; it is a decision aid, not a newly chosen product font. Manrope and Plus Jakarta Sans are self-hosted through page-local `@fontsource-variable` imports. No global font token or stack changed.

No text shadow, blur, stroke, font-smoothing override or other raster-like treatment was introduced. Auth behavior is unchanged.

Verification: Chromium confirmed all three cards have an authored width of 176px, with tops 62/40/30 and transforms −9°/+5°/+9°. For Inter, Manrope and Plus Jakarta Sans, Chromium computed identical heading sizes, 650 weights, tracking and line heights; only `font-family` changed. All three selectors reached their settled active style, the desktop page stayed within 1440x900, and the hidden mobile selector produced no overflow at 390x844. `npx vitest run` passed 430/430, `npx tsc --noEmit` was clean, `npm run lint` was clean after removing the throwaway browser script, and `npm run build` completed successfully.

## 2026-08-15 — Inter selected for login; card fan repositioned

**D153. Inter Variable is the final login-page family.** The controlled comparison made the existing Inter treatment the product-owner choice. The temporary selector, state, comparison test, Manrope/Plus Jakarta Sans imports and both font packages were removed. The accepted typography remains exactly as compared: `Welcome back` at 42px/650/−0.025em/1.1 and `Sign in` at 27px/650/−0.02em/1.15, with Inter inherited through the login surface. This finalizes a page choice without changing the global typography system.

**The complete equal-width fan moves 20px left, and Dynamic Programming moves 12px up.** Authored positions are now Dynamic Programming left 52/top 50, SQL Joins left 226/top 40, and System Design left 396/top 30. Widths, rotations, heights, layer order and title-clearance geometry are unchanged.

Verification: Chromium at 1440x900 computed Inter Variable/650 for both login headings, found no typography-comparison control, and confirmed the three authored positions above; 390x844 remained overflow-free. `npx vitest run` passed 429/429, `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed successfully with only the existing chunk-size advisory.

## 2026-08-15 — Today typography aligned with login

**D154 (supersedes the three-family typography rule and D151's page-scoped limitation). Inter Variable is the app-wide non-code font.** The finalized login family is now the default for every Itera surface, not a page-specific exception. `--font-itera-display` remains as a compatibility token but aliases the same Inter stack as `--font-sans`; this closes every existing display-class escape hatch at the token boundary instead of relying on a visual audit of individual components. Inter Tight is no longer imported or shipped. JetBrains Mono remains the code and technical-metadata family.

Today's greeting and large session-card count now simply inherit the default. The shared navigation wordmark does too, and its former weight 800 is reduced to the login wordmark's 650. Sizes, tracking, layout, and the pixel-tuned session-card geometry are unchanged.

Verification: Chromium computed `Inter Variable` for the login wordmark and heading, the Today wordmark/greeting/session count, and a still-`font-itera-display` Progress heading, confirming the compatibility alias at runtime. The navigation wordmark computes at weight 650; Library's Settings and Filter controls both remain the same Inter 14px/600 treatment. Desktop and 390x844 checks reported no horizontal overflow. `npx vitest run` passed 429/429, `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed successfully with only the existing chunk-size advisory.

## 2026-08-15 — Ordering cards require explicit submission

**D155 (narrows D96 for Ordering). Ordering retains the shared front/back flip presentation, but its surface is not an activation target.** Clicking card padding or other non-control content cannot submit or flip the card, because that gesture is too easy to trigger accidentally while dragging the ordered rows. Only **Submit answer** advances to the results face. The surface therefore has no button role, tab stop, pointer cursor or Enter/Space handler; the row move controls and Submit remain keyboard accessible. Other interaction types keep the shared surface activation behavior.

Verification: Chromium confirmed the Ordering surface computes with no button role and an `auto` cursor; clicking its padding leaves Submit visible and produces no feedback, while clicking Submit flips to the graded face. `npx vitest run` passed 430/430, `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed with only the existing chunk-size advisory.

## 2026-08-15 — Review strip, consistent prompt typography, and Recall composition aligned to `recall-card.png`

**D156. `ReviewTopBar` is the shared full-viewport session strip for both review and study preview.** It is an 80px white rectangle with a bottom border: Exit session left, a bold current/total count at the true center, and the active shortcut right with its key rendered as a bordered `<kbd>`. Recall says **Space** / "to flip" exactly as the locked reference; objective types retain truthful Enter/select/rating hints. `/preview` no longer hides this bar or duplicates it with the old tag row and numeric jump input. Both AppShell-hosted study-preview routes cancel the shell's normal 32px top content padding so the strip meets the navbar with no canvas gap. Editor live previews remain intentionally chrome-free because an unsaved card has no meaningful deck/session position.

**All six interaction fronts now render their prompt through one `CardPrompt` component.** The standard is 24px/700 for every type and viewport. The former Recall-only `sm:text-3xl` exception is removed. Only prompts beyond 280 normalized characters or six non-empty lines fall back to 20px. The same shared predicate drives a visible, non-blocking warning in all six v2 editors: smaller review text will be used, and the author should consider shortening or splitting the card. Saving remains allowed because existing/migrated content must stay renderable.

**Recall uses a balanced three-row face and a reference-shaped flip cue.** Equal top and bottom tracks hold the type pill and cue while the complete prompt block occupies a vertically centered middle track; long content grows the card rather than clipping. The old generic `Copy` glyph is replaced by two small, opposing-angle bordered flashcards built from DOM/CSS, matching the reference silhouette without adding a raster asset or icon dependency.

Verification: Chromium at 1440x900 and 390x844 confirmed the full-width strip, bordered **Space** keycap, centered Recall prompt and overlapping-card cue with no horizontal overflow. All six design-preview interaction fronts computed to exactly 24px and every strip measured the full 1440px viewport. `npx vitest run` passed 435/435 across 64 files, `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed with only the existing chunk-size advisory.

## 2026-08-15 — Review strip width follows the layout viewport

**D157 (corrects D156's full-viewport implementation detail).** `ReviewTopBar` uses `width: 100%` of its available review/preview layout, not `100dvw`. The viewport-unit breakout matched the reference while the page was short, but once a long answer introduced a vertical scrollbar the bar remained as wide as the gutter-inclusive viewport and created a horizontal page scrollbar. The bar still spans the complete immersive Review page and remains wider than the centered card in previews; it simply cannot exceed the usable layout width. Vertical document growth for long answers remains intentional.

Verification: Chromium with a deliberately short 420px viewport produced vertical overflow before and after reveal at both 1440px and 390px, while `documentElement.scrollWidth === clientWidth` in every state (1440/1440 and 390/390 respectively). `npx vitest run` passed 435/435 across 64 files, `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed with only the existing chunk-size advisory.

## 2026-08-15 — Preview strip is full-bleed with navbar-aligned controls and state-aware guidance

**D158 (refines D156 and D157).** The Review strip now separates its full-bleed surface from its aligned content: the outer white header and bottom border span the complete available page width, while an inner `max-w-[1280px]` grid uses the same parent padding and centering as `TopNav`. Exit session therefore shares the Itera logo's left edge, and the shortcut/status shares the profile control's right edge. The `AppShell` main itself is full-width with zero top padding only for `/preview` and `/cards/:id/study`; ordinary app routes retain the standard centered 1280px content main. This is structural layout, not a viewport-unit breakout, so D157's scrollbar fix remains intact.

**Revealed guidance describes the available action rather than restating keys.** A feedback state with rating controls now says **Rate your answer**. A non-committing preview with `hideRating` says **Answer revealed**. The former `1–4 to rate` keycap/hint is removed from the strip; the four rating buttons still carry their own visible numbers and keyboard behavior.

Verification: Chromium at 1440x900 measured both the navbar and Review-strip outer boxes at 1440px, both inner frames at x=80/right=1360, Exit and the Itera logo at x=80, and the revealed hint/profile control at right=1360. At 390x844 both inner frames measured x=16/right=374 after layout settled, and question/revealed states both kept `documentElement.scrollWidth === clientWidth` (390/390). `npx vitest run` passed 438/438 across 64 files; `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed with only the existing chunk-size advisory.

## 2026-08-15 — Review content receives a symmetric bottom frame

**D159. Session-backed Review surfaces reserve 56px below their final content, matching the existing 56px strip-to-card gap.** The spacing belongs to `ReviewSessionScreen`'s outer root rather than `RatingControls`, because ratings are not always rendered and preview-only revealed cards still need a deliberate page ending. It applies only when the session top bar is present; editor live previews that opt out of session chrome do not inherit this page-level padding.

Verification: Chromium measured 56px from the Review strip's bottom edge to the card's top edge and 56px from the revealed rating block's bottom edge to the session root at both 1440x900 and 390x844. `npx vitest run` passed 438/438 across 64 files after an unrelated Matching editor timeout reproduced green in isolation; `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed with only the existing chunk-size advisory.

## 2026-08-15 — Review-strip microcopy and exit affordance finalized

**D160. The exit cue is a literal `<`, not a directional icon, and the entire `< Exit session` button advertises clickability with a pointer cursor.** This follows the locked `recall-card.png` wording more closely and avoids giving a small navigation action more visual weight than its label. The right-side action/status label uses the same Inter size, weight and line height as Exit session; the bordered `<kbd>` remains deliberately smaller and semibold so it still reads as a keyboard key rather than ordinary copy.

Verification: Chromium rendered the literal `<` at 18px and computed the full Exit target as `cursor: pointer`. In the revealed state, Exit session and **Rate your answer** both computed as Inter 16px/500 with a 24px line height, while the keycap retained its smaller treatment. The finalized baseline remains 438/438 tests across 64 files with typecheck, lint and build clean (existing chunk-size advisory only).

## 2026-08-15 — Matching connection badges return to the visual midpoint

**D161 (supersedes D120). A Matching check/X badge belongs at the midpoint of its own connector.** A connection from row 1 to row 3 therefore places its badge horizontally between the two columns and vertically on row 2, matching `matching-card.png`, instead of anchoring every badge 30% from the source item. This applies to every hop of a two- or three-column board and to both answering and graded states.

Crossing connections can still share the same mathematical midpoint. `placeMatchingBadges` groups those collisions and tries the nearest symmetric position along the involved cubic curves until all 24px badges retain 4px of clearance. The group shares one curve parameter so the result stays visually balanced, every badge remains on its own line, and no screen-space nudge disconnects a marker from the relationship it describes. Non-colliding connections remain at the exact 50% midpoint.

Verification: Chromium at 1440x900 measured an isolated row-1-to-row-3 badge at the exact connector midpoint (x 536, y 431, aligned with row 2). A pair of crossing connections that previously shared one midpoint rendered 31.5px apart, clearing two 24px badges; the 390x844 check remained overflow-free. `npx vitest run` passed 441/441 across 65 files, `npx tsc --noEmit` and `npm run lint` were clean, and `npm run build` completed with only the existing chunk-size advisory.
