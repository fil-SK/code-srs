# Current feature / page inventory

The canonical description of **what the product does and how it is meant to behave**, route by route, plus the behavior rules that apply across routes (Tip vs Explanation, study sessions, grading, account/login). It also states plainly what is **planned** and what is **explicitly out of scope**, so a planned feature is never mistaken for a shipped one.

Boundaries: for *implementation status detail* (real vs. placeholder numbers, known issues, next milestone) read [`CURRENT_STATE.md`](CURRENT_STATE.md); for *how it is built* read [`architecture.md`](architecture.md); for *how it should look* read [`design-system.md`](design-system.md); for *why* read [`itera-decisions.md`](itera-decisions.md).

**Status legend**
- **v1, unchanged** — original functionality, not touched by the redesign beyond an automatic color reskin (see below).
- **v1, reskinned only** — same as above, called out explicitly where it matters.
- **Itera redesign, production** — new-IA/new-visual-system work that's live on the real route.
- **Itera redesign, placeholder** — visually built and routed, but the content/logic behind it is illustrative, not real. *(No route carries this status any more; kept because the vocabulary is referenced elsewhere.)*

> Every route rendered through `AppShell` (all "v1" rows below) picks up Itera's colors automatically via `.itera-scope` — see [`docs/design-system.md`](design-system.md) §1. That's a **visual pass only**: data, structure, and behavior are otherwise unchanged from before the redesign started.

| Route | Component | What it does | Status |
|---|---|---|---|
| `/` | `TodayPage` (`src/features/today/`) | Home page: a Suggested Session hero over the **real due queue** (real count, real contributing deck names, an estimate from the learner's own review durations, Start session, and a working **Adjust session** dialog), a four-row Momentum panel (**Current streak · Retention · Due today · Next milestone**), a real Continue Learning list, and a hand-rolled SVG pace chart of **reviews completed per day** over the last seven local days. Renders through the shared `AppShell`/`TopNav` like every other standard route. | **Itera redesign, production.** Every learning value is computed by `src/domain/stats/{todayMetrics,streak,deckMetrics,progressMetrics}.ts` from real `Card`/`Deck`/`ReviewLog` data; the page fetches once and the four panels are presentational. Three honest states: no decks/cards (one empty state pointing at Create a deck / Import), cards but nothing due (a caught-up hero with the real next-due time and no Adjust control), and a normal populated day. Weekly Goal was **removed, not computed** — no goal concept exists. It replaced the pre-Itera dashboard as `/`'s owner; that page has since been deleted. |
| `/decks` | `LibraryBrowserPage` (+ `LibraryCollectionView`) | The Library: Collection nav sidebar (UI-only tree over `Deck.parentId`); All Decks identity, search/filter/sort, ten-row deck pagination, New Deck and a shortcut to the real JSON importer; create/rename/delete. Selecting a Collection renders its separate identity view (rolled-up stats, its decks, any cards filed directly on it). | **Itera redesign, production.** The locked `all-decks.png` formatting applies only to All Decks. Promoted out of a fixture preview onto real `useDecks`/`useSearchCards` data; both the preview it came from and the pre-Itera `DecksPage` have since been deleted. |
| `/decks/:id` | `LibraryDeckPage` | A deck's cards as a table — Cards/Insights tabs, 44px search/type/status/sort controls, always-on seven-row pagination with visible-range copy, drag-reorder within the visible default/manual page, deck settings. | **Itera redesign, production.** One `CardTable` renders every card. **Clicking a row opens the card in study preview** (`/cards/:id/study`); Edit/Duplicate/Move/Suspend/Delete live in the row's kebab menu — see `itera-decisions.md` D113. Deck settings has Save/Cancel actions and closes after a successful save, Cancel, or re-clicking its toggle. There is no separate read-only detail screen. |
| `/roadmaps`, `/roadmaps/:id` | `RoadmapsPage`, `RoadmapEditorPage`, `RoadmapCanvas` | Graphs of decks connected by directed edges to define a learning order; hand-built SVG canvas, no graph library. | **v1, explicitly deferred from the Itera MVP nav** (spec §36) — data/repo/Supabase table untouched. UI may be hidden/retired from nav later, not deleted (see `itera-decisions.md` D17). |
| `/review` | `ReviewPage` → `ReviewSessionV2` | The due queue: presents one card at a time via any of the 6 interaction types, grades via FSRS, persists back onto that card's embedded `scheduling`. Accepts `?deck=<id>` (that deck and its whole subtree) and `?limit=<n>` (the first n cards). The queue is **snapshotted** when the session starts and never rebuilt while it runs. If a grade cannot be saved, the session stops on that card and says so with a **Try again** — it never advances past a review that was not recorded, and the learner does not have to answer or re-grade the card. | **Itera redesign, production.** Renders through the same shell (`ReviewSessionScreen`) as `/design-preview/review/*`. A grade advances the card's scheduling and records one review-history entry as a single all-or-nothing write, so the two can never disagree. Revealed cards with rating controls show **Rate your answer** in the strip; the final content keeps a 56px bottom frame matching the strip-to-card gap. The v1 `ReviewSession.tsx`/`useReviewSession.ts` it replaced have been deleted, so this is the only Review surface. |
| `/preview` | `PreviewPage` | Flip through a deck's cards (or a single card, opened from a card row) with no scheduling impact: the shared Review strip (**< Exit session**, bold position, keycap shortcut), prev/next, and arrow keys. The card itself is the real Review experience — the production `ReviewSessionScreen`, the same path `/review` takes, reading the one `Card` model directly (there is no migration step in front of it any more). | **Itera redesign, production.** The old tag/jump-input strip and edge-aligned page controls are gone. In deck mode, bordered Prev/Next controls flank `X of Y` inside the white strip; Left/Right navigate unless focus is inside an interactive card control or editor. `AppShell` gives this route a full-width, zero-top-padding main, and the strip controls align to the navbar's centered frame. Revealed cards say **Answer revealed** because this route hides rating controls; the final content retains the shared symmetric 56px bottom frame. |
| `/decks/:deckId/cards/new` | `CardCreatePage` → `CardTypeChooser` / one `*EditorShell` per type | Itera Create/Edit (Phase F): the `add-new-card.png` composition as one bordered three-section form. Recall is selected by default; all six interaction types remain switchable with their existing icons. Card content is type-specific; shared Organize controls preserve Deck + comma-separated Tags and add identifying icons. Save/Cancel sit in the footer, while the real production interaction preview remains available as a desktop drawer or narrow-screen tab. Saves a `Card` into the one card store, immediately schedulable in `/review`. | **Itera redesign, production.** All six interaction types are wired up and every pre-existing authoring field remains available. |
| `/cards/:id/edit` | `CardEditEntry` | One switch on `interaction.type` picking the matching editor shell. Saving reuses the card's id/createdAt/scheduling/suspended, so `ReviewLog` history survives an edit. The switch covers every union member, so the fallthrough is a **"Card not found"** state reachable only for an id that resolves to no card. | **Itera redesign, production.** |
| `/cards/:id/study` | `CardStudyPreviewPage` | The Deck row's primary click target: the real Review experience for the card's actual interaction type, against a fresh scheduling baseline — nothing is persisted. Also the closest thing to a "Card detail" view, per `itera-decisions.md` D69. | **Itera redesign, production**, non-committing preview only. |
| `/progress` | `ProgressPage` (`src/features/progress/`) | The real Itera Progress page (mockup-driven): a local sidebar (only "Overview" and "Review history" are live), and exactly five headline KPIs — **Learned, Due, Reviews, Retention, Current streak**. Learned/Due/Streak are current-state values; Reviews/Retention follow the selected date range, and retention changes use percentage points. The page retains the review-activity heat map, a deck-scopable retention-over-time chart with visible null gaps, and derived recent milestones. Deck Performance shows **Deck · Learned · Due · Retention** for leaf/actionable decks, includes due decks with no period reviews, and orders due work first; due rows open their `/review?deck=` scope. | **Itera redesign, production.** Retention uses required pre-grade `ReviewLog.stateBefore`, and Today imports the same definition. The misleading review-count-chunk Trend column was removed. The other seven sidebar rows remain focusable `aria-disabled` "Soon" placeholders; no planned sub-page was added. |
| `/progress/history` | `ReviewHistoryPage` (`src/features/progress/`) | **Review history**: one row per `ReviewLog`, newest first — card, deck, rating, resulting interval, timestamp — filterable by range (30D/3M/1Y/All), deck (including its subtree) and rating, paginated 25 at a time. The chronological record behind Progress's aggregates. | **Production.** Records are immutable; the card/deck metadata beside them reflects the card's *current* metadata, so this is a history view, not a forensic audit trail. |
| `/settings`, `/settings/:section` | `AccountSettingsPage` (`src/features/settings/`) + `SettingsNav` + `sections/*` | The full Account settings page the avatar menu opens, mockup-driven: a local section nav (Profile · Email & password · Appearance · Notifications · Privacy · Connected devices — divider — Import / Export) beside the active section. Only one section is real — **Import / Export** (the JSON backup, moved here verbatim from the old `SettingsPage`, now confirming through `useDialogs` instead of `window.confirm`). Import offers **Merge** and **Replace**; every entity in the file is validated before anything is written, and on the local backend Replace is all-or-nothing, so a failed import leaves the existing workspace exactly as it was. Replace is offered as an `aria-disabled` option with its reason when the cloud backend is active, which cannot roll a failed replace back. Every other section, including the Profile form/statistics/danger zone, is an inert greyed placeholder that states what it will hold. Billing and Plan & usage from the mockup were dropped outright — there is no billing concept in this product. Unknown or missing `:section` falls back to Profile — which is why the account menu no longer links "Spaced repetition (FSRS)" to `/settings/card-scheduling`: that slug does not exist, so the row silently opened Profile. It is now an `aria-disabled` "Soon" row like every other unbuilt destination. | **Itera redesign, production**, with the pre-existing Import / Export carried over unchanged. `SettingsPage.tsx` was deleted, not left unrouted, since its content moved wholesale. |
| `/design-preview/*` | `src/features/design-preview/**` | Chrome-free preview routes exercising the real, reusable v2 Review components against fixture data: `review/{recall,multiple-choice,write-code,ordering,matching,walkthrough}`. | **Itera redesign infrastructure.** A structurally separate top-level route array entry, not nested under `AppShell` — chrome-free by construction, not by hiding chrome with CSS. Each page imports the **production** `ReviewSessionScreen`, so a preview cannot drift from `/review` — which is why these survived while the Library previews (an adapted-not-imported fork) were deleted. |
| `/login` | `LoginPage` (`src/features/login/`) | The sign-in page, built to the `login-v3.png` mockup and compacted to a calmer 1080px desktop surface: a product half (real Itera mark, "Welcome back", a three-card CSS learning-card illustration, and the three icon/label/supporting-copy principles from `login-icons.png`) beside a quieter sign-in panel (email, password with a show/hide toggle, Remember me, Sign in, and Continue with demo workspace). Inter Variable is the finalized page family after a controlled comparison against Manrope and Plus Jakarta Sans. Chrome-free by construction — a top-level route with no `AppShell` ancestor, and outside `RequireAuth`. | **Itera redesign, production shell over dummy local auth.** In local mode the password is never stored, sent, or verified: signing in mints a `LocalSession` (`src/auth/localSession.ts`). When Supabase is configured the password field and Remember me are hidden and Sign in sends the existing magic link. Forgot password is a deliberate `aria-disabled` placeholder — no reset backend exists. |
| *(no route — the guard)* | `src/auth/{RequireAuth,AuthProvider,AuthGate,localSession}` | `RequireAuth` is one pathless layout route wrapping every product route (the whole `AppShell` tree plus `/review`); signed-out visitors are redirected to `/login` carrying the route they wanted. `/login` and `/design-preview/*` sit outside it. `AuthProvider` matches the auth model to the active backend: a Supabase session signs you in when Supabase is configured, a local session when it is not, and never the other way round. `AuthGate` only waits for the Supabase bootstrap. | **Itera redesign, production.** Local mode is gated now — it previously had no login at all. `localStorage`/`sessionStorage` (Remember me picks which) is touched in exactly one file. |

## Notable coexisting states (not bugs)

- **One navigation shell.** Since the App Shell convergence milestone every standard route renders through `AppShell` → `TopNav` (Today · Library · Progress), and `/review`, `/login` and `/design-preview/*` are chrome-free top-level routes by construction. `Sidebar.tsx`/`BottomNav.tsx`/`navItems.ts`/`PageHeaderOverride.tsx`/`TodayShell.tsx` were deleted. (Earlier revisions of this doc described two coexisting shells — that tradeoff no longer applies.)
- **`/roadmaps*` is the only route mounted but unlinked from nav** (deliberately deferred from the MVP IA, spec §36). Everything else that used to be in this state — `/browse`, `/drafts`, `/cards/new`, `/stats` — was deleted on 2026-08-17 rather than given an entry point. An unmatched URL now renders a real "Page not found".
- **Card taxonomy is 6 interactions, and that is the whole model.** All six (Recall, Multiple Choice, Write Code, Ordering, Matching, Walkthrough) have real editors and real review views, and every card lives in one store as one `Card` type. The v1 8-type union it replaced was deleted along with `migrateCard` when the two models converged; a card of any interaction type is schedulable in `/review` the moment it is saved.

---

## Cross-cutting product behavior

These rules apply to every card and every session, regardless of route.

### Card interactions (the six v2 types) — implemented

| Interaction | What it drills | Response | Grading |
|---|---|---|---|
| **Recall** | Free-form recall, code reading, find-the-bug, predict-output, explain-code (one interaction, differentiated by an authoring preset) | Flip to reveal | Self-graded |
| **Multiple Choice** | Single- or multi-correct selection | Select option(s), submit | Exact-set: correct only if every correct option is selected and no incorrect one is |
| **Write Code** | Producing the right code | Type into a code editor | Line-ending normalization + outer trim + per-line trailing-whitespace trim against accepted answers |
| **Ordering** | Correct sequence | Drag, **or** keyboard Move up/down, then explicitly **Submit answer**; clicking the surrounding card surface never submits or flips it | Position-wise (an item counts only at its authored index), with partial-credit score |
| **Matching** | Associating concepts across up to three columns | Assign values across a connected board | Partial credit per cell; a shared column lets one value serve several terms |
| **Walkthrough** | Multi-step scenarios (tracing execution, staged reasoning) | Walk the steps, each with its own response type and optional step-scoped tip/explanation | Per step, dispatched by that step's response type; partial credit |

Every interaction's front prompt uses the same 24px treatment. Prompts over 280 normalized characters or six non-empty lines use 20px in Review and trigger a non-blocking authoring warning to shorten or split the card.

**Grading model — LOCKED.** Objective correctness and recall quality are different things. A correct answer does not automatically mean *Good*; an incorrect one does not automatically mean *Again*. **The learner always chooses the final rating.** The app may pre-select a recommendation after auto-grading, but that recommendation must stay overridable. For automatically validated cards, the **first** submitted response is the scored one — inspecting or experimenting afterward does not change what was recorded.

**Review flow — LOCKED.** Strictly two-phase: **Question → reveal/submit → Answer → one FSRS rating.** Even Walkthrough, which is internally multi-step, produces exactly **one card-level FSRS rating** at the end; the grade bar stays locked until the last step.

### Tip vs Explanation — LOCKED

Two distinct optional fields; do not merge them or use one for the other's job.

| | **Tip** | **Explanation** |
|---|---|---|
| Purpose | Helps the learner *reach* the answer | Adds context *after* the answer |
| When | Before reveal/submission | After reveal/submission, **before** the rating controls |
| Where | Directly below the active card, easy to find | Below the card, separate from the answer itself |
| Collapsible | May be collapsed by the learner, but **never** hidden away in a side panel | — |

Tip is a genuinely new field, authored per card (and, for Walkthrough, per step).

Walkthrough supports both scopes at once. The card-wide tip remains available throughout the unfinished sequence and the card-wide explanation appears after the final step. Independently, the active step may show its own optional tip before that step's first submission, then replace it with that step's optional explanation after submission. Returning to an answered step shows its explanation, not its pre-answer tip. Older Walkthrough records have neither step field and continue to render unchanged.

### Study sessions

A study session is **temporary and generated**, not a persisted entity: sources include all due cards, one deck, or a deck scope passed as `?deck=`. The queue is snapshotted so grading does not reshuffle it mid-session.

**Undo is available only on the end-of-session "All done" screen**, where it steps back one card and reverts the last grade — restoring the card and removing its history entry as one all-or-nothing write, the exact inverse of the grade. A failed undo changes nothing and says so, leaving the review recorded and Undo still offered. There is no mid-session undo: `ReviewSessionV2` renders the Undo control only once the queue is exhausted, and `ReviewSessionScreen`'s key handler covers Escape / Space / Enter / 1–4 only. The `useUndoGrade` mutation itself is fully general — it is the in-session UI entry point that is missing (see "Capabilities removed with the v1 surface" below).

**Not implemented:** the `StudySession` type exists in `src/types/review.ts` but nothing constructs or stores one, so there is no session goal (card count / target minutes), no session id on history rows, and no resumable session. Progress's "sessions" are **gap-clustered from review timestamps** for display purposes, not read from a session record.

### Account and login — implemented

Signing in is real and gates the whole product: `/login` mints either a Supabase session (magic-link OTP) or a local `LocalSession`, and every product route sits behind one guard. **In local mode the password is a dev/demo shell — never stored, sent or verified**, and "Continue with demo workspace" is a first-class way in. There is no password authentication and no password reset anywhere in the product; "Forgot password" is a deliberate `aria-disabled` placeholder. The account menu links the live Account settings and Import / Export surfaces, and signing out is available there whenever the active mode has a session; unbuilt menu destinations remain focusable "Soon" rows.

---

## Planned (specified, not built)

Do not describe any of these as shipped, and do not build against them as though they exist.

| Feature | State |
|---|---|
| **Weekly Goal** | Removed from Today rather than fabricated. There is no persisted or configurable weekly target anywhere in the product, and none was added — a real one needs a goal entity, a settings surface, and a definition of what counts toward it. |
| **Richer milestones / achievements** | Today's Next milestone is a *derived* in-progress-deck continuation, recomputed each render. There is no milestone or achievement entity, no mastery milestones, no topic completion, no streak or review-volume awards, no custom goals. Progress's Recent milestones are likewise derived from `ReviewLog` thresholds. |
| **Advanced session customization** | Adjust session covers deck scope and card count only. Time-boxed sessions ("15 minutes"), weak-cards-only, new-vs-review splits, difficulty filters, interaction-type filters, tag filters and custom FSRS parameters are all deferred, and nothing is abstracted in anticipation of them. See [`TODO.md`](TODO.md). |
| **Collection as a real entity** | "Collection" is derived in the UI from `Deck.parentId`. There is no `Collection` type, table or migration — see [`itera-migration-plan.md`](itera-migration-plan.md) §6. |
| **Onboarding and full empty-state coverage** | No onboarding flow exists. Empty states exist for Library and Progress; the full audited set does not. |
| **Progress sub-pages** | Decks, Activity, Review lag, Milestones, Achievements, Stats, Reports are `aria-disabled` "Soon" rows. Overview and Review history are live. "Activity" is kept free on purpose for a broader feed (cards created, decks edited, imports) rather than being consumed by review history. |
| **Settings sections** | Profile, Email & password, Appearance, Notifications, Privacy, Connected devices are inert placeholders. Only Import/Export is real. |
| **Mobile navigation pattern** | The shared top nav simply scrolls horizontally; no dedicated mobile navigation exists. Matching's stepwise mobile pairing flow is likewise unbuilt. |
| **Dark mode** | Deferred, and now explicitly declared absent: `index.html` and `getInitialTheme()` both say light, and the unreachable `[data-theme='dark']` token block plus the inert `dark` variant were deleted. `ThemeProvider`/`useTheme` remain (CodeView/CodeEditor pick their syntax palette from `Theme`), so re-adding it means re-authoring 16 tokens and a toggle. |
| **Media in cards** | Not developed. When it arrives it must store local asset ids (never fragile absolute paths), be referenced from `RichContent`, be included in export archives, and work offline. |

### Capabilities removed with the v1 surface (candidates for redevelopment)

These worked on the pre-redesign app and became unreachable when `/browse`, `/cards/new`, `/drafts` and `/stats` were deleted on 2026-08-17. **They were deliberately not reimplemented in that pass** — the decision was to record them and redevelop later, on the redesigned surfaces, if they prove wanted. Each row names the host that would own it, not the deleted file.

| Capability | Where it lived | Host if redeveloped |
|---|---|---|
| **Cross-deck full-text card search** | `/browse`, the only caller that passed `text:` into `repo.cards.query` and so the only consumer of `domain/search/searchableText.ts` (still present in both backends) | Library. Today `/decks` searches deck names/descriptions only, and `/decks/:id` searches one deck by title only. |
| **Tag filter on cards** | `/browse`'s tag chips | The `/decks/:id` Cards toolbar, which already has search / type / status / sort but no tag control. Tags are still authored and displayed — nothing filters by them. |
| **Mid-session undo of a grade** | The pre-redesign session header button plus the `u` shortcut | `ReviewSessionScreen` / `ReviewTopBar`. `useUndoGrade` already exists and is wired to the completion screen only. |
| **Flip back to the question after reveal** | the pre-redesign card view (Space, or a "Show question" control) | `reviewPhase.ts`, which has no `feedback → presenting` transition. The two-phase flow is deliberately one-way today, so this is a product decision, not just wiring. |
| **Due forecast (next 7 days)** | `/stats`, via the deleted `computeStats.ts` | `/progress`. Its "Soon" Activity and Review-lag rows are the natural home; `progressMetrics.ts` has no forecast equivalent. |
| **Drafts quick-capture inbox** | `/drafts`, plus the `?draft=` handoff into the old editor | No host yet. **The data is fully intact** — `repo.drafts`, the Dexie store and backup import/export all survive, and `useDrafts` is retained unused for exactly this reason — so this is UI work, not a migration. |

Two smaller affordances went the same way and are recorded here rather than in their own rows: the pre-Itera deck list had per-row **Study** and **Flip-through** actions (the Deck page's overflow menu now owns flip-through) and a one-click **new subdeck** control (nesting is now set through Deck settings' Parent deck field). Separately, `codeReading` and `bugFinding` are no longer authorable as distinct types — they are stored as-is but open, and re-save, as `recall`.

## Explicitly out of scope

Deliberately **not** built during this redesign. The architecture should not block them, but nothing should prebuild them either.

Marketplace · payments · deck publishing and version updates · collaborative deck editing · AI generation or grading at runtime · semantic code equivalence · code compilation or execution · a debug-simulation card type · compact power-user density · rich media authoring beyond basic local support.

**Roadmaps** is a special case: it is a real, working, shipped feature (`/roadmaps`, `/roadmaps/:id`) that is deliberately **excluded from the redesigned navigation** and receives no new development. Its data — the `Roadmap` type, both backends' repo methods, the Supabase table, backup inclusion — is preserved. Hiding the UI is allowed; **deleting the data requires separate explicit approval** and is not scheduled.

**Cloud sync/accounts** was listed as deferred in the original spec but is partly real: Supabase sync with magic-link auth predates the redesign and still works, and a session boundary now gates the app in both modes.
