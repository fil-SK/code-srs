import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function BrowsePage() {
  return (
    <PagePlaceholder
      title="Browse"
      note="Search and filter across all cards, with type badges, tags, and bulk actions. Built in M4."
    />
  )
}
