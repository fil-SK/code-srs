import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function ReviewPage() {
  return (
    <PagePlaceholder
      title="Review"
      note="The core review loop — present, reveal/answer, grade, next — with flip-to-reveal cards and FSRS scheduling. Built in M2 (engine) and M3 (all card types)."
    />
  )
}
