import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      note="Appearance, FSRS scheduler parameters, and JSON import/export (your backup and migration path). Built out across M6–M7."
    />
  )
}
