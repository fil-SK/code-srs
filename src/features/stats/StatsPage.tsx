import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function StatsPage() {
  return (
    <PagePlaceholder
      title="Stats"
      note="Reviews per day, per-deck mastery, retention, and due forecast — all derived from the review log. Built in M7."
    />
  )
}
