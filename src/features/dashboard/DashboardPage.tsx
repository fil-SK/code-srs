import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      note="Due counts, streak, retention, and your deck list land here as the data layer and review engine come online (M1–M4)."
    />
  )
}
